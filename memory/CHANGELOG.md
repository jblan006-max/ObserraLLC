# Obserra EIOS — CHANGELOG

## 2026-06 — Agentic AI Security Control Plane v1 (product pivot)
- **Visual pivot to "Obserra — Agentic AI Security Control Plane" (v1)**. Legacy SAP nav fully removed from the UI (backend SAP endpoints untouched — shared backend). Nav now: Executive Overview · AI Security → Agentic AI Security · Sources → Connector Health · Admin. Persistent **v1** badge in header + on Executive Overview.
- **Executive Overview (`/app` index)** rebuilt into `AIExecutiveOverview.jsx` — a complete AI rollup: 8 KPI cards (navigate to Control Plane), delegated-authority chart, guardrail coverage, highest-risk agents, toxic combinations, shadow AI queue, open incidents. Every card opens the universal deep-dive; grounded AI Analyst headline. (`SapOverview` retired from the index route.)
- **Tool Toxicity Map** (`ToxicityMap.jsx` + `lib/agenticToxicity.js`): visual Agent→Tool→Permission→Resource graph flagging toxic capability combinations (dangerous/action tools without matching guardrail). Embedded in the Authority & Tools tab; nodes open a deep-dive.
- **Universal deep-dive across all agentic dashboards** (`lib/agenticDeepDive.js`): agent/system/incident cards in Authority, Shadow AI, Incidents and the Executive Overview are clickable into `RiskDetailModal` with rating, facets, compliance refs and recommended actions.
- **Kill Switch runtime enforcement** — `POST /api/agents/{ref}/enforce` (admin, `agents.py`): suspend→restricted / kill→killed / resume→sanctioned; sets `enforcement` object + `enforced` flag, writes the Defensibility Ledger (`risk_engine._ledger`), posts Slack/Teams alert (`self_scan._post_chat_alert`), and dispatches to `org.agent_runtime_webhook` when configured. `AgentDetailModal` shows Suspend/Kill/Resume + live enforcement-status pill.
- **Shadow AI Discovery feed** — `POST /api/ai-systems/discover` (admin, `routes.py`): idempotent hybrid seed of common public GenAI SaaS + shadow-flagged agents → populates the Shadow AI queue. "Run discovery" button in the Shadow AI tab.
- **Advisor grounding** — `ai_advisor._build_context` now loads the live `ai_agents` collection (tools, permissions, guardrail %, dangerous tools, toxic combos, red-team score). Defensive `.get()` on `ai_systems` (fixed a `drift` KeyError after discovery).
- **Board Brief Scheduler** — admin picks weekly/monthly cadence + "Email now" (`BoardBriefControl.jsx`); `GET/PUT /api/agents/board-brief/schedule`, `POST /api/agents/board-brief/send` (Resend HTML brief). Cron send folded into the existing weekly-drift-digest (weekly) and monthly-board-report (monthly) crons — no new cron slot.
- **Corner Obserrian Advisor**: `AppShell` now mounts the eye-logo `AIAdvisor` (bottom-right, grounded on `/api/advisor/chat`) instead of `SapAdvisor`.
- **Copy**: onboarding tour + `scripts/gen_docs.py` (PDF/DOCX guides) rewritten to Agentic AI Security (no SAP wording); guides regenerated.
- Verified: backend 10/10 pytest (iteration_89) + curl; frontend e2e (nav, v1, toxicity map, KPI nav, shadow discovery, kill switch suspend/kill/resume with status pill, board brief).


## 2026-06
- **Official branding everywhere** (only logo used): `/public/brand-mark.png`, `brand-wordmark.png` (cropped from official lockup), `brand-lockup.png`. Regen: `/app/scripts/gen_brand.py`.
- **PWA**: install banner (animation + 7-day "Later" snooze), branded splash, maskable icons, adaptive favicons, iOS splash (portrait + landscape iPad).
- **Onboarding tour**: role-aware (exec/ops), spotlight on `[data-testid=mode-toggle]`, a11y (role=dialog/aria/Esc/non-persist backdrop), per-user persistence, Replay from Settings.
- **Board report PDFs** (`reports.py`): badge + watermark + footer on all exports. Board report adds cover page, **light/dark theme**, **exposure trend line** + **top-risks bar chart**, **AI Executive Summary** callout, auto **Key Takeaways**.
- **Quarterly Deck** (`layout:"deck"`): 5 landscape slides (cover, KPI snapshot, trend, risks, takeaways), theme-aware. Frontend "Deck" button + Dark/Light toggle in Board Report modal.
- **Report Recipients**: admin-only `GET/PUT /api/reports/recipients` (email-validated) → `org.report_recipients`; monthly cron emails branded PDF to admins/execs + extras.
- **Send Test Email**: `POST /api/reports/test-email` (admin/exec) generates + emails the branded PDF to self; Settings "Send me a test now" button.
- **Per-org Rebranding**: `GET/PUT /api/reports/branding` (admin) — custom company name + logo (base64, ≤~1.5MB) on all board outputs; defaults to Obserra. `_resolve_brand(org)` threads a brand object through `_build_pdf` + deck; Obserra watermark dropped when custom. Settings "Report Branding" card.
- Scheduled board PDF: cron attaches branded PDF (cover + charts + takeaways).

## Verified
- iteration_14.json frontend 6/6 PASS.
- iteration_15.json: backend 16/16 PASS + frontend 100% (report/deck × light/dark, recipients GET/PUT + 403, branding GET/PUT + 403 + reset, test-email + 403, Settings cards, modal PDF/Deck downloads). No bugs. Regression suite: /app/backend/tests/test_iter15_board_reports.py.

## Notes / gotchas
- Twice, large `search_replace` edits to `reports.py` went stale in-memory (404s); fix = re-apply + `sudo supervisorctl restart backend`, verify via curl.
- Board branding logo b64 payload uses /app/frontend/public/logo-mark-192.png in tests — keep that asset.
- Non-blocking polish backlog: validate logo MIME/magic bytes; return dropped invalid emails to UI; explicit "Remove logo"; consider splitting reports.py PDF helpers.

## 2026-08-07 — Sidebar categories + Owner all-access
- Left nav regrouped into labeled sections: 6 paid categories (AI Governance, Cyber Risk, Third-Party Risk, Asset Intelligence, Audit & Evidence, Reporting & Board) each rendered with a distinct accent color header (ai/crit/high/primary/med/low), colored dot, and a matching colored left-border wrapping the nested dashboards to signal dependency. Plus muted General "Dashboards", "Admin", "Account" sections. (`AppShell.jsx` NAV_SECTIONS + CAT_STYLE)
- Owner full unrestricted access: `OWNER_EMAILS` (default jblan2026@gmail.com) in `auth.py`. `is_owner()` forces role=admin in get_current_user, bypasses require_active_subscription, and `/api/subscription` + `/api/modules` return enterprise plan + all entitlements + all modules owned — hardcoded on login, no payment ever required.

## 2026-08-07 — 15-char passwords, self-serve SSO, team access roles, collapsible nav
- Password policy raised to 15-char minimum (auth.py PASSWORD_POLICY_MSG + validate_password_policy). Frontend hints added on register (Auth.jsx) and forced reset (ForcePasswordReset.jsx, min check 8→15). Verified: 12-char register → 400, 15-char → 200.
- Self-service Apple + Enterprise SSO config: new security.py (Fernet, APP_SETTINGS_KEK) + sso_config.py (GET/PUT /api/admin/sso, admin-gated, secrets encrypted at rest in app_config/_id=sso). social_auth.py now resolves Apple/OIDC creds from DB with env fallback; /api/auth/providers reflects DB config. New SsoCard.jsx in Settings (admin) for Team/Service/Key IDs + .p8, OIDC discovery/client id+secret, SAML metadata URL. Verified PUT/GET/clear + providers toggle.
- Team access roles: POST /api/auth/team/{id}/access sets per-user module_access (null=all). team_members returns module_access. /api/subscription computes effective per-user entitlements + `restricted` flag; AppShell owns() no longer grants enterprise bypass when restricted. Team.jsx: Access column + modal with 6 category checkboxes / All-access toggle.
- Collapsible sidebar: every named section header is a toggle (chevron) with per-section state persisted to localStorage (obserra-nav-collapsed); 6 paid categories keep their accent colors.

## 2026-08-07 — SSO test, access presets, invite-with-access, collapse-all, billing seats
- Test SSO Connection: POST /api/admin/sso/test (admin) validates an OIDC discovery URL (fetch + endpoint check) or an Apple key (ES256 client-secret build). "Test connection" buttons added in SsoCard. Verified good→ok, bad→friendly error.
- Access Presets: org-level presets (auth.py GET/POST/DELETE /api/auth/access-presets). Team access modal gets "Apply preset" dropdown + "Save as preset"; invite form gets a preset/custom selector.
- Invite With Access: InviteBody + team_invite accept module_access; invite form lets admins pick All / a preset / custom categories up front.
- Collapse All: sidebar top control toggles all sections collapsed/expanded (ChevronUp/Down), persisted to localStorage.
- Billing Seats & Access: GET /api/billing/access-summary (admin) → per-pack owned + seat_count/total_members; new card on Billing lists all 6 packs with "X of N teammates". Verified via curl + screenshot.

## 2026-08-07 — Preset manager, seat drill-down, access history, bulk access, access-change emails
- Preset Manager: Team page "Access Presets" card lists saved presets with rename (POST new + DELETE old) and delete.
- Bulk Access: member selection checkboxes (desktop + mobile) + a bulk bar; POST /api/auth/team/bulk-access applies an access preset/all to many teammates at once.
- Access Audit Trail: _log_audit now stores a `target` field; GET /api/auth/team/{id}/access-history returns team.access/team.invite entries for that member, shown as "Recent changes" in the access modal.
- Seat Drill-Down: Billing seats rows are expandable to show the exact teammates who hold each pack's access (uses access-summary `seats`).
- Access-change emails: _notify_access_change emails the teammate (managed Resend) + in-app notification whenever their dashboard access changes (single or bulk). Verified via curl (set/bulk/history) + screenshots.

## 2026-08-07 — Preset inline edit, history PDF export, notify toggle, seat search
- Preset On Invite Reuse: Preset Manager chips now have an inline category editor (SlidersHorizontal → modal with 6 checkboxes / all-access) plus rename & delete — no need to open a teammate.
- History Export: GET /api/reports/access-history/{id}.pdf builds a branded ReportLab PDF of a teammate's access changes; "Export PDF" button in the access modal (verified: application/pdf, ~213KB).
- Notify Toggle: AccessBody + BulkAccessBody now accept notify (default true); access modal has "Email this teammate about the change" and the bulk bar has "Email teammates" so admins can run quiet migrations. Verified bulk notify:false via curl.
- Seat Search: Billing "Seats & Access" card has a search box that filters/auto-expands packs to show which packs a specific teammate can reach.

## 2026-08-07 — Access diff email, bulk-from-directory, preset usage count, history filters
- Access Diff Email: _notify_access_change now takes old_ma and shows added (green) vs removed (red) packs in the teammate email; set/bulk endpoints pass the pre-change access.
- Bulk From Directory: Team page "Quick select role" buttons (Operational/Executive/Admin) select every member of a role in one click, then apply a preset via the bulk bar.
- Preset Usage Count: each Preset Manager chip shows "N in use" (teammates whose access exactly matches the preset).
- History Filters: the access modal's Change History has actor + since/until date filters (client-side); Export PDF forwards them as query params to GET /api/reports/access-history/{id}.pdf (actor/since/until). Verified: filtered PDF returns valid 213KB.

## 2026-08-07 — Preset sync, audit-log filters, CSV import, access expiry, monthly access review
- Sync Preset Members: pin a teammate to a preset (AccessBody.pin / bulk pin_preset, user.preset_pin); editing a preset auto-updates every pinned teammate's access + emails them. Access modal has a "Sync: <preset>" dropdown; member rows show a pin badge.
- History In Audit Log: AuditLog page now has actor + since/until filters over /api/audit-logs (which now carries `target`), surfacing all team.access/invite/preset events.
- CSV Team Import: POST /api/auth/team/import bulk-invites from name,email,role,preset rows (emails each teammate); Team page has a paste-CSV card.
- Access Expiry: AccessBody.expires_on stores an expiry + revert value; daily-drift-digest cron runs _run_access_expiry to auto-revert lapsed grants (clears pin) and email the teammate. Access modal has an expiry date picker; member rows show an expiry badge.
- Monthly Access Review digest: monthly-board-report cron runs _run_access_review, emailing admins a per-pack seat snapshot.
- Fix: repaired a corrupted duplicated tail in auth.py seed_admin (IndentationError) and re-applied team_members/set_member_access.

## 2026-08-07 — Connectors auto-connect on save (no blocking test)
- All 5 live connectors (M365, Copilot, ChatGPT/OpenAI, Teams, SAML/SSO) now go LIVE/READY the instant an admin saves credentials — `live_connectors.py` PUT handlers always set `live=True`/`valid=True`. Real data (Graph user/risky counts, Copilot seats, OpenAI model count, SAML entity_id) is still pulled best-effort but wrapped in try/except so an unreachable/failed check never blocks the connection. Status text now "Connected — …".
- Frontend `AvailableConnectors.jsx`: buttons relabeled "Save & connect" / "Connecting…"; toasts report "connected" (no more "NOT LIVE"/"Invalid" error path for saved creds).
- Apple + Enterprise OIDC + SAML in Settings (`SsoCard.jsx`) already save-and-enable instantly (Test connection is optional); `/api/auth/providers` reflects configured state immediately.
- Catalog connectors (Okta/AWS/Azure/CrowdStrike/Splunk/ServiceNow/Wiz) remain one-click MOCKED demos (no credential fields).
- Verified iteration_28: frontend 100% — all pills flip LIVE/READY on save with dummy creds, disconnect reverts to NOT SET, Settings SSO badges show Connected without Test. Owner org left clean.

## Jun 2026 — Owner/Board crons + Ticket Deep-Link + "Assigned to me" lens + SoD state consolidation (iteration_72)
- Backend crons verified: `run_sap_owner_digest` (weekly per-owner SoD email) + `run_sap_board_pack` (monthly exec pack + analytics PDF) — script `_t_crons.py` passed (log created, digest sent, no errors).
- Ticket Deep-Link: watchlist ticket badge → modal with full ServiceNow stage timeline via `GET /api/sap/ticket/{number}`.
- "Assigned to me" watchlist lens: `watchlist-mine-toggle` filters pinned areas to owner == logged-in email (`useAuth`).
- State consolidation refactor: new `context/SodContext.jsx` (`SodProvider`/`useSod`) replaces 50+ prop pass-throughs to the 5 SoD cards. Regression clean (iteration_72 frontend 100%).


## Jun 2026 — Owner Leaderboard + Board-Pack on-demand + Ticket live-refresh + Digest deep-links (iteration_73)
- Owner Accountability Leaderboard: `GET /api/sap/watchlist/leaderboard` + `SodOwnerLeaderboard.jsx` — ranked owners by open Critical SoD, unowned-Critical tile, expandable per-area breakdown.
- Board Pack on-demand: `GET /api/sap/board-pack/preview` + admin `POST /api/sap/board-pack/send` + `SodBoardPackCard.jsx` (preview modal + one-tap send; records monthly log).
- Ticket timeline Live Refresh: watchlist ticket modal polls `/sap/ticket/{number}` every 4s while open ("auto-refreshing" chip).
- Owner Digest Deep-Links: `_owner_digest_html` area rows link to `/app/sod?wl=<area>`; watchlist scrolls to + ring-highlights that card.
- Verified frontend 100% (iteration_73). Fixed a transient `cardRef` runtime error from a silent partial edit.


## Jun 2026 — Leaderboard Nudge + Assign-from-Leaderboard + Board-Pack Scheduling + Timeline Toast (iteration_74)
- Leaderboard Nudge: admin `POST /api/sap/watchlist/leaderboard/nudge` + "Nudge all owners" button (confirm-gated) — emails each owner their hot spots now.
- Assign From Leaderboard: leaderboard `unassigned` areas + inline owner-email/Assign → `/sap/watchlist/remediate`, live-refreshes the board.
- Board Pack Scheduling: `sap_board_pack_config` + `GET/PUT /api/sap/board-pack/config`; inline enable/day/recipients editor + header chip; cron gates on enabled AND day-of-month.
- Timeline Toast: ticket modal toasts when a live-refreshing change advances a stage.
- Verified frontend 100% (iteration_74). Baseline reseeded (kept Finance pin).


## Jun 2026 — Walkthroughs & setup guides rebuilt for SAP UAC (with screenshots)
- `scripts/capture_shots.py`: captures 14 live SAP UAC screens (incl. Watchlist/Owner-Leaderboard/Board-Pack). Reinstalled Playwright Chromium 1234.
- `scripts/gen_docs.py`: SAP UAC Install & User Guide → PDF (16 pages) + DOCX (14 images) in `backend/assets/docs/`, verified by rendering.
- `deploy.py` + `Settings.jsx`: SAP UAC download filenames + email/section copy; auto-refresh pipeline unchanged.
- `OnboardingTour.jsx`: EXEC/OPS steps rewritten for SAP UAC (SoD Command Center focus).

## Fix — In-App Tour never mounted (Jun 2026, screenshot-verified)
- Root cause: `OnboardingTour` was defined but never imported/rendered anywhere, so first-login auto-show and the Settings "Replay tour" event had no listener, and the step preview screenshots never appeared.
- Fix: mounted `<OnboardingTour />` in `AppShell.jsx` (beside `<SapAdvisor />`). Restores auto-show on first login, Settings → Replay tour, and the SAP UAC dashboard preview image on every step.
- Verified via screenshot: tour opens on both paths; `tour-preview` image loads (naturalWidth 1440) on step 1 (Welcome) and step 2 (Executive Mode, spotlight on mode toggle).

## Feature — One-click "Regenerate tour images" (Jun 2026, curl + screenshot-verified)
- Admins can recapture the in-app onboarding tour previews (`/tour/{overview,sod,watchlist,monitoring}.jpg`) so they always match the current UI after a redesign.
- `scripts/capture_shots.py`: added `SHOT_TOUR_ONLY=1` mode → captures only the 4 tour-mapped screens then copies to `frontend/public/tour/`.
- `backend/deploy.py`: `regenerate_tour_images()` + `POST /api/deploy/regenerate-tour` (admin-only, runs capture in a threadpool so it doesn't block the event loop; returns the refreshed image list).
- `Settings.jsx`: "Regenerate tour images" button beside "Regenerate guides" (180s client timeout).
- Note: this forked pod had Playwright browser build 1208 but the Python package expects 1234 — reinstalled chromium 1234 to `/pw-browsers`. Endpoint verified: 200, all 4 images refreshed in ~19s.

## Feature — One-click "Refresh all visuals" + hardened capture (Jun 2026, curl + screenshot-verified)
- Added an admin **Refresh all visuals** button (Settings → Deployment & Documentation) that recaptures every dashboard screenshot once, then rebuilds BOTH the in-app tour previews and the PDF/Word guides in a single pass. It calls the existing `POST /api/deploy/regenerate-guides?capture=true` (a full capture already refreshes tour images via `_copy_tour_shots`, then `gen_docs.generate_all()` rebuilds the guides). 300s client timeout.
- Hardened `scripts/capture_shots.py` with `_settle(page)` — waits for `networkidle` + the app's lazy-route Suspense spinner (`main .animate-spin`) to clear before screenshotting. Root cause it fixes: a fixed 2.2s wait could capture the loading spinner (blank ~8.9KB JPEGs) if a route hadn't finished loading (e.g. during a dev hot-reload). Post-fix captures are full-content 115–171KB.
- Verified: full capture+rebuild endpoint 200 (PDF ~2.5MB, DOCX ~1.86MB); `overview.jpg` renders the live Executive SAP Access Risk dashboard; both new buttons render; tour preview shows the refreshed screenshot.

## Feature — Self-contained one-click on-premise installer (Jun 2026, zip + YAML verified)
Rebuilt the on-premise package into a true one-click, SAP-UAC-branded installer (was "Obserra EIOS", deploy-files-only, and had a broken build context).
- `backend/deploy.py` `_build_onprem_zip()`: now bundles the FULL app source — `backend/` + `frontend/` (excludes node_modules, __pycache__, .git, build, all `.env`, `assets/docs`, `.pyc`) — plus the deploy files, a generated `.dockerignore`, and a root-level executable `install.sh`. Zip root `obserra-sap-uac/`; served as `Obserra-SAP-UAC-OnPrem.zip` (also the email attachment name). Verified: 342 entries, 6.6MB, 0 leaked secrets/junk, install.sh mode 0755.
- `deploy/onprem/install.sh` (root of package): genuine one-click — checks Docker/Compose, creates `deploy/.env` from template with an auto-generated `JWT_SECRET`, `up -d --build`, polls health, prints URL + first-login + promote-to-admin mongo one-liner. Cross-platform sed + openssl/urandom fallback.
- Fixed broken Docker build contexts (original `./backend` resolved to a non-existent `deploy/backend`, and the frontend `COPY ../deploy/nginx.conf` was outside its context). Now: backend `context ../backend` + `dockerfile ../deploy/backend.Dockerfile`; frontend `context ..` + `dockerfile deploy/frontend.Dockerfile` (COPYs `frontend/` and `deploy/nginx.conf`). Applied across all three compose files (plain + Caddy HTTPS + Traefik HTTPS) — all validated as well-formed YAML.
- Rebranded every package file to "Obserra SAP UAC" (INSTALL.md, .env.example `DB_NAME=obserra_sap_uac`, Caddyfile, Dockerfiles, compose headers, volume `obserra_uac_mongo`). LLM key left blank by default (AI optional). `Settings.jsx` copy/button updated. Docker isn't available in the pod, so build was validated statically (YAML + zip structure), not via an actual `docker build`.

## Feature — Installer hardening: version stamp, offline mode, seed admin, background refresh, CI (Jun 2026, curl + screenshot-verified)
Five follow-ups on the on-prem installer (user picked all four Next Actions + the CI check):
- **Version stamp**: new `/app/VERSION` (1.0.0); shared `scripts/onprem_pack.py` embeds `VERSION` + `BUILD_INFO` (version + build date) in the package and names the download `Obserra-SAP-UAC-OnPrem-v{ver}-{date}.zip`; `install.sh` prints the version/build banner. Verified filename header: `...-v1.0.0-2026-08-09.zip`.
- **Offline mode**: bundled the pure-python `emergentintegrations` wheel in `deploy/onprem/wheels/` (removes the private index need); `backend.Dockerfile` installs from `/wheels` (and, if `deploy/wheels/OFFLINE` exists via the new `build-wheelhouse.sh`, installs EVERYTHING with `--no-index` for fully air-gapped hosts). Backend build context switched to package root across all 3 compose files so wheels are reachable.
- **Seed admin (auth)**: new `GET /api/auth/bootstrap-status` + `POST /api/auth/bootstrap-admin` (creates the first admin + enterprise/active org ONLY when the users collection is empty; 409 otherwise — inert in hosted). `install.sh` prompts for admin email/password and calls it. Consulted integration_expert (custom JWT/bcrypt playbook) before writing. Verified: status `initialized:true`, bootstrap `409` in hosted.
- **Background refresh**: `POST /api/deploy/refresh-visuals` returns immediately and runs the full recapture+guide rebuild in a FastAPI BackgroundTask, then writes an in-app "Visuals updated" notification; `Settings.jsx` button is now fire-and-forget with a toast. Verified: 202 → tour images refreshed → "Visuals updated" appeared in the notification bell.
- **CI verify**: `scripts/assemble_onprem.py` (reuses `onprem_pack.iter_files`, assembles 343 files identical to the zip), `Makefile` (`make package` / `make verify`), and `.github/workflows/onprem-verify.yml` that `docker compose build`s the package on every push — the real "does it boot" check Docker-in-pod can't provide.
- `backend/deploy.py` zip builder refactored to the shared `onprem_pack` module (DRY with the CI assembler). Backend reload clean; ruff clean (no F821/syntax); frontend compiles.

## Feature — Release tagging, update notifier, health endpoint, seed data, GHCR release (Jun 2026, curl + screenshot-verified)
- **Release tagging**: `read_version()` now honors an `OBSERRA_VERSION` env override; new `scripts/stamp_version.py` writes `VERSION` from the latest git tag; the CI workflow stamps it so every download/image is traceable. Verified env override → `2.3.4` and versioned zip name.
- **Update notifier**: `GET /api/deploy/version` returns `{current, latest, update_available, notes, url}` (best-effort fetch of `UPDATE_MANIFEST_URL`, cached 6h, sends a User-Agent so CDNs/ingress don't 403 it). New admin-only `UpdateBanner.jsx` mounted in `AppShell` shows a dismissible banner (dismissal stored per-version). Verified end-to-end with a temp manifest: banner rendered "v9.9.9 available — you're on v1.0.0" + Release-notes link + dismiss.
- **Health endpoint**: unauthenticated `GET /api/health` pings Mongo → `{status, service, version, db}`; `install.sh` now polls it (instead of the SPA root). Verified `{"status":"ok","version":"1.0.0","db":true}`.
- **Seed sample data**: `POST /api/deploy/seed-demo` (admin) + a `seed_demo` flag on `bootstrap-admin` load the realistic SAP dataset for the org (idempotent) so a fresh self-hosted instance shows populated dashboards; `install.sh` prompts "Load the demo SAP dataset?". Verified seed-demo → 46 persons / 54 accounts.
- **GHCR + release asset**: new `.github/workflows/release.yml` (on `v*` tags) stamps version, assembles, builds & pushes prebuilt `backend`/`frontend` images to GHCR (lowercased prefix), and attaches the versioned `.zip` as a Release asset; new `deploy/onprem/docker-compose.ghcr.yml` runs those prebuilt images (no local build). All 6 workflow/compose YAMLs validated.

## Feature — Release-notes modal, one-click upgrade, deep health, demo reset, public manifest (Jun 2026, curl + screenshot-verified)
- **Release-notes in-app**: `UpdateBanner` now has a "What's new" button → modal showing version, notes and a `changelog` list before upgrading. Screenshot-verified ("What's new in v9.9.9" + changelog).
- **One-click upgrade**: `POST /api/deploy/upgrade` (admin) runs `docker compose pull && up -d` in the background for GHCR/compose deployments — opt-in via `ONPREM_UPGRADE=1` + mounted Docker socket/compose; returns a clear 400 otherwise (verified in hosted). Modal "Pull latest & restart" button wired with a graceful toast.
- **Deep health check**: `GET /api/health` now returns `checks` = db latency, org count, connectors connected/total, and scheduler (`cron_configured` + `last_activity`). Verified: db 0.7ms, 18 orgs, 108/126 connectors, scheduler active. Still includes top-level `status`/`db` so `install.sh` grep keeps working.
- **Demo reset**: `POST /api/deploy/reset-demo` (admin) wipes the org's `sap_*` + demo collections and reseeds a clean populated instance; Settings has a confirm-gated "Reset to demo dataset" button. Verified → 46 persons / 54 accounts.
- **Public releases manifest (OOTB banners)**: `release.yml` publishes `manifest.json` to GitHub Pages and pre-fills `UPDATE_MANIFEST_URL` in the shipped `.env.example` with the Pages URL, so self-hosted instances show update banners with zero config. `.env.example` gains `UPDATE_MANIFEST_URL` / `ONPREM_UPGRADE` / `ONPREM_COMPOSE`.


## Feature — System Health page + version badge; Google login error surfacing (Jun 2026, curl + screenshot-verified)
- **System Health page** (`frontend/src/pages/SystemHealth.jsx`, route `/app/system-health`, Admin nav item): live deep-health tiles from `GET /api/health` (Service, Database+latency, Connectors connected/total, Scheduler armed + last activity, Organizations, auto-refresh every 20s), Version + one-click Upgrade panel (`GET /api/deploy/version`, `POST /api/deploy/upgrade`) with a streaming progress log polling `GET /api/deploy/upgrade/status`, and a Backups panel (list/`POST /api/deploy/backup`/restore/download). Admin-gated content; non-admins see vitals only.
- **Version badge** in the sidebar footer (`AppShell.jsx` `VersionBadge`): shows running `vX.Y.Z` with a pulse dot + "update →" link (admins → System Health) when a newer release is available.
- Backend for this task was already complete (deep `/api/health`, `/deploy/version`, streaming `_upgrade_job`, nightly `backup_all_orgs` folded into the daily cron). Verified: health 200 (db 0.7ms, 18 orgs, 108/126 connectors), backup created (38 collections/3722 docs), page renders end-to-end.
- **Google login "bounced back to login" fix**: root cause was a 403 (Google email had no matching Obserra account) whose message the frontend discarded, so the user returned to a blank login screen with no explanation. `AuthCallback` now forwards the backend reason to `/?auth_error=…`; `Auth.jsx` renders a clear red banner (`data-testid=auth-oauth-error`); the backend 403 now names the exact Google email attempted. Owner `jblan2026@gmail.com` maps correctly (200). Screenshot-verified banner; 401/400 error paths confirmed via curl.

## Feature — System Health hardening: restore guardrail, health alerts, backup metadata, header pill (Jun 2026, curl + screenshot-verified)
- **Restore confirmation + pre-restore auto-backup** (`deploy.restore_org` + `/api/deploy/restore`): a restore now first snapshots current data (tag `pre-restore`) so a rollback can never lose live data; endpoint requires `confirm: "RESTORE"` (400 otherwise). UI: typed-"RESTORE" modal (`sh-restore-modal`, `sh-restore-confirm-input`) with an auto-backup note; toast names the pre-restore file. Verified: 400 w/o confirm, 200 w/ confirm returning `pre_restore_backup`.
- **Health alerts** (`deploy.evaluate_org_health` + `run_health_alerts`, folded into the daily 08:00 cron; admin `POST /api/deploy/health-alert-run`): evaluates DB (ping), scheduler (cron secret), and per-org degraded connectors; on any issue posts a Slack/Teams alert (`_post_chat_alert`) + in-app notification (deduped daily). Verified: healthy → no alert; endpoint returns `webhook_configured`/`alerted`.
- **Backup download guardrail**: backups now carry `org_name`, `docs`, `collections`, `tag` via a `.meta.json` sidecar written on creation; System Health table shows Organization + Contents columns + a tag badge so admins grab the right snapshot. Fixed a silent revert of `_list_backup_files` and a second-precision filename collision (now microsecond + uuid suffix). Verified via API + disk.
- **Header health pill** (`AppShell.jsx` `HealthPill`, admin-only, polls `/api/deploy/health-detail` every 30s): green "Systems healthy ✓" / amber "Degraded" / red pill on every page, links to System Health. Screenshot-verified.

## Feature — Backup schedule, alert routing, degraded drill-down, 24h uptime strip (Jun 2026, curl + screenshot-verified)
- **Backup schedule control** (`GET/PUT /api/deploy/backup-config`, stored on `org.system_health.backup`): admins toggle automatic backups, choose Daily/Weekly frequency, and set snapshots-to-keep (1–90). `backup_all_orgs` now honours per-org config (weekly = skip if a backup <6.5d old); manual `/backup` prunes to the configured keep.
- **Alert routing matrix** (`GET/PUT /api/deploy/health-config`, stored on `org.system_health.alerts`): per event type (DB / Connector / Scheduler) choose Slack / Teams / Email. `run_health_alerts` + `health-alert-run` route via `_route_alert` (Teams MessageCard, Slack text, email to admins/execs); in-app notifications always recorded.
- **Degraded drill-down**: `evaluate_org_health` now returns `degraded_detail` (cid, resolved name, state, detail, checked_at); System Health shows a red "Degraded connectors" panel with a one-click **Re-probe** per connector (`POST /api/connectors/{cid}/test`). Header pill turns amber "Degraded" and links here.
- **24h uptime strip**: `health_history` collection records throttled samples (every ~12 min via `/health-detail`, plus nightly); `GET /api/deploy/health-history?hours=24` powers a status-dot strip with % healthy. 7-day retention.
- Note: the earlier `health-alert-run` payload gained a `routing` field; the standalone `_post_chat_alert` health call was replaced by per-type routing.

## Feature — Backup encryption, uptime range toggle, test-alert (Jun 2026, curl + screenshot-verified)
- **Backup encryption at rest** (`GET/PUT /api/deploy/backup-encryption`, `POST .../disable`): admin sets an org passphrase; data key = PBKDF2(passphrase, salt) wrapped with a `JWT_SECRET`-derived master (Fernet) so nightly backups encrypt unattended. Snapshots stored as Fernet ciphertext (verified: file magic `6741`, not gzip). Restore requires the passphrase — verified 400 (missing) / 403 (wrong) / 200 (correct). `_list_backup_files` exposes `encrypted`; UI shows a lock + a restore-passphrase field + an Enable/Disable encryption modal.
- **Uptime range toggle**: `/health-history` cap raised to 720h and retention to 30 days; UptimeStrip has 24h/7d/30d buttons and buckets samples (48/84/60 columns, worst-status-wins) for a clean fixed-width strip.
- **Test alert**: `POST /api/deploy/health-alert-test` fires a sample Slack/Teams/email message via `_route_alert`; "Send test alert" button next to Save routing reports which channels were attempted. Verified (slack/teams configured true, email attempted).
- Note: restore signature gained `passphrase`; backups now written as raw bytes (gzip or Fernet(gzip)) — old gzip-only snapshots remain restorable (backward compatible).

## Feature — Passphrase rotation, restore preview, compliance evidence PDF (Jun 2026, curl + testing-agent verified)
- **Passphrase rotation** (`POST /api/deploy/backup-encryption/rotate` {old_passphrase, new_passphrase}): verifies the current passphrase against the org verifier, derives a new data key (new salt), master-wraps it, and re-encrypts every existing encrypted snapshot in place (rewrites each `.meta.json` salt/wrapped_key). Returns `{enabled:true, reencrypted:N}`. Verified: old passphrase invalidated (403), new passphrase works (200), reencrypted=1.
- **Restore preview** (`POST /api/deploy/restore-preview` {file, passphrase?}): non-destructive per-collection Current→Backup record-count diff (`{rows:[{collection,current,backup,delta}], collections, total_backup, total_current}`). Reads/decrypts the snapshot but writes nothing; encrypted backups require the passphrase (400 w/o, 403 wrong). UI: "Preview changes" button inside the restore modal renders a diff table before the typed-RESTORE confirm.
- **Compliance evidence PDF** (`GET /api/deploy/compliance-evidence`, reportlab): auditor-ready signed PDF with org/generated-by/version header, System Health & Controls table (status, DB, scheduler, backups, latest backup, backup policy, at-rest encryption), Access Governance Coverage counts (persons, accounts, mitigations, certifications, ServiceNow tickets, watchlist, auto-remediation), an attestation paragraph, and a SHA-256 document-integrity signature. Verified valid PDF (`%PDF-` … `%%EOF`).
- **Context note**: the frontend UI for all four features was already built in the prior session, but the three backend endpoints above had been dropped by a silent revert; they were re-added this session consistent with the existing encryption/backup/restore helpers. Alert Digest (daily bundled degraded-events digest per channel) was already intact in `run_health_alerts`.

## Feature — Evidence Locker, monthly evidence email, restore-preview log, digest preview, tour auto-skip (Jun 2026, curl + testing-agent 9/9)
- **Evidence Locker** (`POST /api/deploy/evidence/generate`, `GET /api/deploy/evidence/list`, `GET /api/deploy/evidence/download`): generates a signed compliance PDF via shared `_generate_compliance_pdf`, archives it to `EVIDENCE_DIR` (`/app/evidence`) with a `.meta.json` sidecar (generated_by, source manual/monthly-cron, size, SHA-256), keeps the latest 60. UI: `sh-evidence-panel` with "Generate & archive" + a by-date table with per-row download.
- **Monthly evidence email** (`GET/PUT /api/deploy/evidence-config`, stored on `org.system_health.evidence.monthly_email`, default on): `_run_monthly_evidence_email` folded into the existing `monthly-board-report` cron (1st, 08:00 UTC) — generates + archives + emails the signed PDF to admins/execs + saved `deploy_recipients`. No new cron added (platform max 5 respected). UI: `sh-evidence-monthly` toggle.
- **Restore preview log** (`GET /api/deploy/restore-previews`): every SUCCESSFUL `restore-preview` now writes to `db.restore_previews` (by, at, file, encrypted, collections, total_backup/current, net_delta, top-5 deltas) as a dry-run audit trail. UI: `sh-restore-log-panel` list (shows current→backup + Δ). Encrypted-without-passphrase previews still 400 and are not logged.
- **Digest preview** (`GET /api/deploy/health-digest-preview`): shows what today's bundled per-channel health digest WOULD send (or "already sent"/"healthy") without sending. UI: `sh-digest-preview` button + `sh-digest-preview-result` panel in the alert-routing card.
- **Tour auto-skip**: `OnboardingTour.jsx` no longer auto-opens when `window.location.pathname` starts with `/app/system-health` (fixes the welcome tour overlaying the admin console; replay still works via the event).
- Refactor: the ad-hoc `GET /api/deploy/compliance-evidence` download and the cron now share `_generate_compliance_pdf(org_id, generated_by)`.

## Feature — Evidence retention, custom report period, digest send-now, auditor share links, QR verification (Jun 2026, curl + testing-agent 7/7)
- **Evidence retention** (`GET/PUT /api/deploy/evidence-config` now carries `keep`, 1–365, default 60): Evidence Locker prune honors the configured count; UI "Keep latest N report(s)" input (`sh-evidence-keep`) saves on blur.
- **Custom report period** (`POST /api/deploy/evidence/generate` accepts `{period:{kind:'month'|'quarter'|'current', value}}`): `_resolve_period` computes the label + date range; the PDF gains a "Reporting period" row and a period "Uptime (period)" figure from `health_history`. UI period dropdown (`sh-evidence-period`) offers Current + last 6 months + last 4 quarters.
- **Auditor share links** (`POST /api/deploy/evidence/share` → token+expiry; public `GET /api/deploy/evidence/shared/{token}` streams the PDF inline, no auth, tracks opens, honors expiry with 410): UI per-row Share button (`sh-evidence-share-*`) opens `sh-share-modal` with a copyable read-only URL.
- **QR verification**: every archived (locker) PDF embeds a QR + verification URL; `_make_and_archive_evidence` binds a token to the file's final SHA-256 in `db.evidence_verify`; public `GET /api/deploy/evidence/verify/{token}` renders a branded HTML page ("Authentic document" + expected SHA-256; bad token → 404). UI per-row verify link (`sh-evidence-verify-*`).
- **Digest send-now** (`POST /api/deploy/health-digest-send`): admin sends today's bundled per-channel digest to Slack/Teams/email on demand (healthy → `{sent:false,reason:'healthy'}`). UI "Send digest now" button (`sh-digest-send`) renders in the degraded branch of the digest preview.
- Refactor: ad-hoc download, Evidence Locker generate, and the monthly cron all route through `_make_and_archive_evidence`/`_generate_compliance_pdf`. Note: QR uses reportlab's built-in `barcode.qr` (no new dependency). Retention currently set to 45 in the demo org (testing left it there; user-configurable).

## Feature — Verification watermark, digest test recipient, share-link manager, quarterly pack, Audit Room (Jun 2026, curl + testing-agent 7/7)
- **Verification watermark** (`_watermark_pdf` via pypdf 6.15 + reportlab overlay): shared PDFs (`evidence/shared/{token}`) and audit-room PDFs get a faint diagonal "VERIFIED COPY" / "AUDIT ROOM COPY" stamp + share date/expiry; fails open to the original bytes if unavailable.
- **Digest test recipient** (`POST /api/deploy/health-digest-test-email` {email}): sends a one-off health digest to a single address (sample text when healthy); UI email input + "Send test" in the alert-routing panel (`sh-digest-test-email`/`sh-digest-test-send`).
- **Share Link Manager** (`GET /api/deploy/evidence/shares`, `POST /api/deploy/evidence/share/revoke`): "Shared Access Links" panel (`sh-links-panel`) lists active auditor share links + audit rooms with created-by, expiry and open counts, plus one-tap revoke (`sh-share-revoke-*` / `sh-room-revoke-*`).
- **Scheduled Quarterly Pack** (`quarterly_pack` in evidence-config, default on; `_run_quarterly_evidence_pack` folded into the monthly-board-report cron): on the 1st of Jan/Apr/Jul/Oct, auto-generates + archives + emails a signed quarter-end evidence PDF for the just-ended quarter. UI toggle `sh-evidence-quarterly`. `saveEvidenceCfg` now preserves all three fields so toggles don't reset each other.
- **Audit Room** (`POST /api/deploy/audit-room`, `GET /api/deploy/audit-rooms`, `POST /api/deploy/audit-room/revoke`; public `GET /api/deploy/audit-room/{token}` HTML portal + `/evidence` watermarked PDF): a branded, expiring, no-auth portal bundling live findings (30d uptime, degraded connectors, SoD violations from `db.risks`, ServiceNow tickets, certifications) + the latest signed evidence PDF. UI "Audit Room" button (`sh-audit-room-create`) reuses the share modal for the link.
- Dep added: `pypdf==6.15.0` (frozen into requirements.txt). Note: 2 silent same-file reverts (icon import + loader defs) and a stray duplicated JSX block were caught and fixed during implementation.

## Feature — Audit Room findings depth, room branding, revoke-all, auditor comments (Jun 2026, curl + testing-agent 79 100%)
Delivered the final 4 backlog items for the external-auditor experience:
- **Room Findings Depth**: `_audit_findings()` (deploy.py) now imports `_correlate` from `sap_engine` and returns the **top open SoD violations** (top 8 by severity — rule/area/holder) plus **overdue access certifications** (Active campaigns past due) and a live **open SoD count** (replaces the old `db.risks` count). The public portal renders a severity-coloured SoD table + an overdue-certs list.
- **Room Branding**: `GET/PUT /api/deploy/audit-room-branding` (stored on `org.system_health.audit_room`) — dedicated **logo upload + welcome note**, with a "fall back to my report-branding logo" toggle (`_resolve_room_logo` reuses `org.report_branding.logo`). Portal shows the logo + welcome note. UI: `sh-room-branding-btn` → `sh-brand-modal` (`sh-brand-logo-input`, `sh-brand-use-org`, `sh-brand-welcome`, `sh-brand-save`).
- **Revoke All**: `POST /api/deploy/shares/revoke-all` deletes every evidence share link AND audit room for the org in one tap. UI: `sh-revoke-all` button (confirm-gated) in the Shared Access Links panel header.
- **Auditor Comments**: public `POST /api/deploy/audit-room/{token}/comment` (JS-fetch form on the portal; honours expiry) stores to `db.audit_room_comments` and **notifies admins/execs (in-app + email via managed Resend)**. Admin `GET /api/deploy/audit-room-comments` powers a new `sh-comments-panel` on System Health (author · timestamp · comment). Comment count tracked on the room doc.
- Backend curl-verified end-to-end (room create, branding GET/PUT, public comment POST + admin GET, revoke-all removing 2 links + 3 rooms) and testing_agent iteration_79 backend 100% / frontend 100%, no bugs. `_esc_html` added for safe HTML escaping in the portal. Minor non-blocking note: the public comment endpoint accepts JSON only (portal uses fetch) — form-encoded fallback intentionally not added.

## Feature — Audit Requests inbox (reply+status), room analytics, evidence provenance watermark, expiry reminder + perf caching (Jun 2026, curl + testing-agent 80 100%)
Delivered all 4 Next Action Items + the "Audit Requests" inbox enhancement:
- **Comment Reply + Audit Requests inbox**: each auditor comment now carries `id` + `status` (Open/In Progress/Resolved) + `reply`. Admin endpoints `POST /api/deploy/audit-room-comments/{id}/reply` (auto-sets Resolved) and `.../{id}/status`. The System Health "Auditor Comments" panel is rebuilt as an **Audit Requests** inbox (`sh-comments-panel`) with an open-count chip, per-row status dropdown (`sh-comment-status-{i}`), inline reply box (`sh-comment-reply-input-{i}` / `sh-comment-reply-send-{i}`) and the resolved reply shown inline. The reply + status also render on the public portal as a **Comment thread** so the auditor sees the governance response.
- **Room Analytics**: the Shared Access Links room row shows `sh-room-analytics-{token}` — opens, last-viewed, download count, last downloader.
- **Evidence provenance watermark**: `GET /api/deploy/audit-room/{token}/evidence?who=<name>` stamps "Downloaded by <name> · <UTC date/time> · expires …" onto the PDF; the portal has a `#dlname` name input that appends `?who=`; downloads increment `downloads`/`last_downloaded_at`/`last_downloaded_by`.
- **Auto-expire reminder**: `_run_audit_room_expiry_reminders()` (folded into the daily-drift-digest cron — no new cron slot) emails admins/execs + in-app notice ~3 days before a room expires, deduped via `expiry_reminder_sent`.
- **Performance (user directive: don't run slow when processing jobs)**: the unauthenticated public portal was recomputing the full SoD correlation engine + health eval + 30-day uptime on EVERY open. Added a 60s TTL `_FINDINGS_CACHE` per org (`_audit_findings`) → portal warm ~0.11s. Added `_EV_RAW_CACHE` (immutable evidence files) to skip disk reads on repeat downloads; `_watermark_pdf` now `compress_content_streams()` + `compress_identical_objects()` (defensive). Watermark CPU ~5ms warm; download time is network-bound (~0.34s) with a tiny 35KB payload. Verified via curl + testing_agent iteration_80 (backend 100% / frontend 100%, no bugs). Note: one silent revert of the `view_audit_room` comments-fetch was caught and re-applied.

## Feature — Legacy backfill, SLA badges, room renew, auditor reply-notify (Jun 2026, curl + testing-agent 81 100%)
Delivered all 4 Next Action Items + the "extend room wired to the reminder" enhancement:
- **Legacy Comment Backfill**: `GET /api/deploy/audit-room-comments` now lazily assigns `id` + `status: "Open"` to any pre-existing comment missing them, so every historical comment is replyable/trackable from the inbox.
- **Request SLA Badges**: `slaAge()` helper renders a colored "waiting <n>h/<n>d" badge (`sh-comment-sla-{i}`) on every non-resolved request (green <24h, amber 24–72h, red >72h); disappears once resolved.
- **Room Renew + reminder loop**: `POST /api/deploy/audit-room/renew` extends `expires_at` (default +14d, capped 90) and clears `expiry_reminder_sent` so the reminder re-arms. Frontend Renew button (`sh-room-renew-{token}`) on each room row. The expiry-reminder email now includes a "Open System Health to renew" button linking to `FRONTEND_URL/app/system-health` — closing the loop from heads-up email → one-click renew.
- **Auditor Reply Notify**: the public portal comment form gained an optional email input (`#cmail`, stored as `author_email`). When an admin replies (`reply_audit_room_comment`), if the auditor left an email we send them the governance reply via managed Resend so they don't re-check the portal.
- Verified end-to-end via curl (backfill, renew extending Aug 11→Aug 23, portal email stored, reply auto-resolve + email capture) and testing_agent iteration_81 (backend 100% / frontend 100%, no bugs). Fixed a single-quote escaping bug in the `#cmail` placeholder during implementation. Minor non-blocking reviewer notes: index-based comment testids and non-optimistic portal thread append (left as-is).

## Feature — Reply templates, inbox filters, overdue digest, portal live-thread, oldest-open banner + CSP fix (Jun 2026, curl + browser + testing-agent 82)
Delivered all 4 Next Action Items + the "oldest open request banner":
- **Reply Templates**: `REPLY_TEMPLATES` chips (`sh-reply-templates-{i}`) on each Audit Requests row — one tap fills the reply box with canned text (Evidence attached / Under review / Please clarify / Resolved).
- **Inbox Filters**: status filter (`sh-inbox-status-filter`) + room filter (`sh-inbox-room-filter`, shown when >1 room); `filteredComments` drives the list with an empty-state note (`sh-inbox-empty`); the open-count chip stays total.
- **Overdue Digest**: `_run_overdue_request_digest()` (folded into the daily cron) emails admins/execs a daily summary of requests open past SLA (72h) with a link to System Health, deduped via `system_health.overdue_digest_date`.
- **Portal Live Thread**: the auditor's just-submitted comment now appends instantly to `#thread-list` (XSS-safe `textContent`) with an Open badge, the textarea clears and a thank-you shows — no reload. Thread section restructured with `#thread-h`/`#thread-list` containers.
- **Oldest Open Request banner** (`sh-oldest-open-banner`): a top-of-page banner appears when any request has waited >24h, showing the oldest one and smooth-scrolling to the inbox on click.
- **CSP FIX (important)**: the global `default-src 'none'` CSP in `server.py` was blocking ALL inline scripts/styles/`data:` images on the backend-rendered auditor portal, breaking the comment-submit and PDF-download buttons. Fixed by scoping the middleware: `text/html` responses (the public audit portal + verify page) now get `script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self'; form-action 'self'` while every JSON/PDF API keeps the strict `default-src 'none'`. Verified in a real browser (comment appends live, no CSP violation) + CSP headers confirmed per content-type via curl. testing_agent iteration_82 passed the 4 admin features (banner, templates, filters, regression) and flagged this CSP bug, now resolved.

## Feature — Bulk resolve, saved reply editor, CSV export, portal reply-alert + overdue→Slack/Teams (Jun 2026, curl + testing-agent 83 100%)
Delivered all 4 Next Action Items + the overdue-digest Slack/Teams routing:
- **Bulk Resolve**: `POST /api/deploy/audit-room-comments/bulk-status` (ids+status, capped 500). Inbox rows have checkboxes (`sh-comment-select-{i}`); selecting reveals a bulk bar (`sh-bulk-bar`) with status dropdown + Apply/Clear. Toast reflects the real `modified_count`.
- **Saved Reply Editor**: `GET/PUT /api/deploy/reply-templates` (stored on `org.system_health.reply_templates`, blank rows dropped, `_DEFAULT_REPLY_TEMPLATES` fallback). 'Templates' button → `sh-template-modal` (label + text rows, add/remove); saved set drives the reply chips (`activeTemplates`).
- **Request Export**: `GET /api/deploy/audit-room-comments/export.csv` → full log (submitted, room label, auditor, email, status, request, reply, replied-by/at) as UTF-8-BOM CSV; 'Export CSV' button (`sh-inbox-export`) via `blobDownload`.
- **Portal Reply Alert**: the auditor portal thread now shows a blue '`N` new repl(y/ies) from the governance team' banner (`#reply-alert`) on first visit, tracked client-side via `localStorage[obserra_seen_replies_<token>]` vs the server-rendered reply count; hides once seen.
- **Overdue digest → Slack/Teams**: `_run_overdue_request_digest` now also posts to the org's Slack/Teams webhooks via `_route_alert(org, {slack, teams}, ...)`, so an aging request pings the same channels the team already watches (in addition to the daily email).
- Verified: curl (templates GET/PUT blank-drop, bulk-status, CSV w/ BOM, portal reply-alert markers) + testing_agent iteration_83 (backend 7/7, frontend bulk/templates/export/portal-alert all pass, no bugs; new test file `tests/test_iter83_audit_batch.py`). Non-blocking reviewer note: `SystemHealth.jsx` is ~1340 lines and could be split into components later.

## Feature — Select all, template variables, Slack reply-action links, request analytics (Jun 2026, curl + browser + testing-agent 84 100%)
Delivered all 4 Next Action Items + the response-time metric:
- **Select All**: `sh-select-all` header checkbox selects the whole *filtered* view (`toggleSelectAll`) for one-click bulk actions.
- **Template Variables**: saved replies may contain `{auditor}`, `{period}`, `{date}` which `applyVars()` substitutes on chip insert (auditor→request author, period→latest evidence period label, date→today); the Templates modal shows a hint.
- **Slack/Teams Reply Action**: since we only have outbound webhooks, the overdue-SLA Slack/Teams alert now embeds a per-request tokenized link (`action_token`) to a public page `GET /api/deploy/req-action/{token}` (rendered via `_req_action_html`) with 'Send reply & resolve' and 'Just mark resolved' — `POST .../reply` (sets reply_by='Slack/Teams action', notifies the auditor) and `POST .../resolve`. Invalid tokens show a styled 'Request not found' page.
- **Request Analytics**: `GET /api/deploy/audit-request-analytics` returns org-wide + per-room **median response hours** (auditor comment → governance reply) and open/in-progress/resolved counts. Frontend strip (`sh-request-analytics` + `sh-request-analytics-rooms`) with a proportion bar; re-fetched after every reply/status/bulk action.
- Verified: curl (analytics org {total:3,open:1,resolved:2,median:0.4h} + per-room; action page render + reply resolves & notifies), browser (invalid-token page), testing_agent iteration_84 (backend 100% / frontend 100%, template-var substitution confirmed 'Hi {auditor}…'→'Hi Marcus Webb (KPMG)…'). Non-blocking reviewer notes: `SystemHealth.jsx` ~1378 lines (split later); status-filter option casing vs 'all' initial (works).

## Feature — Configurable SLA targets, weekly-median trend, select-across-filters + performance pass (Jun 2026, curl + digest unit-test + testing-agent 85 100%)
Delivered the final 4-part audit batch, then a speed/loading/upload/download optimization pass.
- **SLA Target Setting** (org default + per-room override): backend `GET/PUT /api/deploy/sla-config` (org `system_health.sla_hours`, 1–720) and `PUT /api/deploy/audit-room/{token}/sla` (per-room `sla_hours`, null=inherit). Helpers `_org_sla` / `_sla_map`. New `sh-sla-panel` card on System Health — org-hours input (`sh-sla-org-input`/`sh-sla-org-save`) + per-room rows (`sh-sla-room-input-{token}`, `sh-sla-room-clear-{token}`) showing effective h + override/default label.
- **Overdue digest rewrite**: `_run_overdue_request_digest()` no longer uses a hardcoded 72h — it computes each org's effective SLA via `_sla_map` (org default + per-room override) and flags only requests open past THEIR target. Verified by a patched-outbound unit test.
- **Action-link expiry**: the Slack/Teams tokenized resolve/reply links minted by the digest now carry `action_expires_at` (`_ACTION_TTL_DAYS = 7`); `_action_expired()` gates `/req-action/{token}` (page shows 'Link expired', POSTs 410). Digest re-mints an expired token.
- **Analytics Trend**: `/api/deploy/audit-request-analytics` already returned `trend[]` (weekly median response hours, last 8 ISO-weeks); added a crash-proof CSS/SVG `Sparkline` component + `sh-request-trend` row (renders 'Not enough history yet' until ≥2 weeks of replies).
- **Select Across Filters**: `sh-select-across` link (shown when every filtered row is selected) flips the bulk bar into across-mode; Apply calls `POST /api/deploy/audit-room-comments/bulk-status-filter` (status + filter_status + room_token → `update_many` across the whole matching set, not just loaded rows). Changing a filter clears the selection.
- **Performance pass** (user: 'reoptimize for speed, loading, upload, downloads'):
  - *Loading*: `src/lib/routePrefetch.js` + AppShell NavLink `onMouseEnter/onFocus/onTouchStart` prefetch the target route's lazy chunk so navigation renders with no Suspense spinner (webpack dedupes with the App.js `lazy()` import).
  - *Upload*: `onBrandLogo` downscales raster logos client-side to ≤512px (canvas → PNG/JPEG q0.85) before base64 upload — much smaller payload; SVGs kept as-is.
  - *Downloads*: `_build_onprem_zip()` now memory-caches the 6.6MB zip keyed by source version+mtime (rebuild only on change); guide PDF/DOCX FileResponses set `Cache-Control: private, max-age=300` (helps on-prem/other deploys — the hosted ingress overrides it to no-store, which is expected). Backend JSON was already gzipped + <130ms.
- Baseline confirmed already fast: hot read endpoints ~90–130ms, gzip on. Verified: testing-agent iteration_85 backend 13/13 + frontend 100% (new pytest `tests/test_sla_and_bulk.py`); org SLA restored to 72h, room overrides cleared, one request left Open. Non-blocking: pre-existing tooling-injected `<span>`-in-`<option>` hydration warning in SodWatchlist (not from this batch).



## Feature — SLA escalation, per-room SLA heatmap, digest scheduling + code split (Jun 2026, curl + digest unit-tests + testing-agent 86 100%)
Shipped the four follow-up items and split the two oversized files.
- **Code split (backend)**: extracted the ~985-line auditor-governance cluster out of `deploy.py` (2796→1814) into a new focused module `deploy_audit.py` (~1020). Shared low-level helpers (`_now_iso`, `_route_alert`, `evaluate_org_health`, `_period_uptime`, evidence helpers, `TokenBody`, the `deploy_router`) stay in `deploy.py` and are imported by `deploy_audit`; `deploy.py` re-imports the two cron fns at the bottom so `server.py`/`scheduled.py` imports are unchanged. Verified: 76 routes intact, health 200, all audit endpoints + public portal 200.
- **Code split (frontend)**: moved the pure presentational pieces (`HealthTile`, `UptimeStrip`, `Sparkline`) + the new `SlaHeatmap` into `src/components/systemhealth/panels.jsx`; `SystemHealth.jsx` imports them. The audit/evidence concerns share the "Shared Access Links" panel + share-modal + reply vars, so a deeper state lift was intentionally deferred to avoid fragmenting shared surfaces (documented).
- **SLA Escalation**: org config `system_health.escalation = {enabled, contacts[], multiplier(1-5, default 1.5)}`. Endpoints `GET/PUT /api/deploy/escalation-config` (emails validated + lower-cased + deduped). The overdue digest also escalates requests open past `multiplier × SLA` to the contacts by email + Slack/Teams (deduped per request via `escalated_at`). New `sh-escalation` UI. Unit-tested: escalated_at set + escalation email sent.
- **SLA Heatmap**: `/api/deploy/audit-request-analytics` returns per-room + org `on_time / breached / pending / on_time_pct / sla_hours` (replied-within-SLA = on-time, replied-late or open-past-SLA = breached, open-within-SLA = pending). New `SlaHeatmap` component renders an 'All rooms' + per-room stacked bar with on-time %.
- **Digest Scheduling**: org config `system_health.digest_schedule = {enabled, days[0-6 Mon..Sun]}`. Endpoints `GET/PUT /api/deploy/digest-schedule`. Digest self-gates per org. NOTE: platform allows max 5 crons and all 5 are used, so the digest runs on the daily 08:00 UTC cron — per-**hour** scheduling isn't possible without retiring another cron; days-of-week + enable are fully functional and the UI states the fixed 08:00 UTC send time.
- Verified: testing-agent iteration_86 backend 22/22 + frontend 100%; state restored to demo defaults. Non-blocking: pre-existing tooling-injected span-in-option hydration warning in SodWatchlist.

## Feature — Heatmap drilldown, weekly escalation rollup, per-hour digest, Exec SLA banner (Jun 2026, curl + testing-agent 87 100%)
Final follow-up batch (user: "package it all up and finish, no more features").
- **Heatmap Drilldown**: `/api/deploy/audit-request-analytics` rooms now include `token`; `SlaHeatmap` rows are clickable buttons — clicking a room sets the inbox room filter to that room + status "Open" and scrolls to the inbox; "All rooms" resets to all.
- **Escalation weekly rollup**: new `_run_weekly_escalation_rollup()` (folded into the `weekly-drift-digest` cron) emails/Slacks escalation owners a 7-day summary of what escalated + median hours from escalation→resolution, deduped per ISO week. Resolutions now stamp `resolved_at` (per-comment status, bulk-status, bulk-status-filter, reply, and Slack/Teams action endpoints).
- **Per-Hour Digest**: freed a cron slot by folding the monthly Studio report into `monthly-board-report` (runs `_run_studio_reports` with quarterly detection); replaced `monthly-studio-report` with a new **`hourly-overdue-digest`** cron (`0 * * * *`). The overdue digest self-gates per org to the configured UTC **hour** + days (`digest_schedule.hour` 0-23). New hour `<select>` (sh-sched-hour) in the UI; removed from the daily-drift cron. Still 5 crons (platform cap).
- **Exec Overview SLA banner**: new `GET /api/deploy/audit-sla-banner` → {open_overdue, breached_7d, escalated_7d}; SapOverview shows a `sap-sla-banner` linking to System Health when any count > 0 (hidden at 0).
- Fixed: an earlier digest-schedule GET/PUT edit had silently not persisted (old `run_hour_utc` version) — re-applied and verified hour persists.
- Verified: testing-agent iteration_87 backend 16/16 + frontend 100% (`tests/test_batch87.py`); configs restored (digest enabled/all-days/hour 8, escalation off, org SLA 72h, one request left Open). Non-blocking: pre-existing dev-overlay span-in-option console warning (not a code bug).


## Obserra Agentic AI Security Control Plane — frontend-first module (Jun 2026, iteration_88 — frontend 100%)
- Built exactly per the user's uploaded PowerShell build spec: a single page at route **/app/agentic-ai-security** with 7 internal tabs — Mission Control, Agent Inventory, Authority & Tools, Guardrails & Red Team, Shadow AI, Incidents, Defensibility. NO backend changes (reuses existing APIs).
- New files: `lib/agenticAIModels.js` (client-side MODELLED intelligence: modeledAgentRiskScore, authorityTier Autonomous/Approval Required/Tool Assisted/Observe/Disabled, guardrailCoverage, summarize/distribution helpers, boardReportBlocks), `hooks/useAgenticAIData.js` (fans out to /agents, /dash/ai-analytics, /ai-systems, /ai-incidents, /workflows, /connectors/health; degrades gracefully with sourceStatus), `components/agentic-ai/` (shared.jsx + 7 dashboards + AgentDetailModal + RegisterAgentModal), `pages/AgenticAISecurity.jsx`.
- Reused endpoints: GET/POST /api/agents, PATCH /api/agents/{ref}, POST /api/agents/{ref}/redteam, GET /api/dash/ai-analytics, GET/PATCH /api/ai-systems, GET /api/workflows, GET /api/connectors/health, POST /api/studio/report/pdf, /api/advisor/insight + /explain (AIInsight/AIExplain).
- STRICT honesty (per spec): DataClassBadge FACT / MODELLED / HEURISTIC BASELINE / AI RECOMMENDATION; red-team labelled HEURISTIC BASELINE (not live adversarial); guardrail/status changes explicitly stated to update governance record only, NOT external runtime enforcement; Defensibility tab surfaces per-source LIVE/UNAVAILABLE truthfully.
- Wiring: new "AI Security" nav section in AppShell (nav-agentic-ai-security, Bot icon, ai accent), route accent 330 81% 60% (rose), lazy route in App.js.
- Seeded 3 REAL agents via POST /api/agents for the owner org (was live_only/empty): AGT-001 Invoice Reconciliation (sanctioned/High), AGT-002 IT Support Copilot (restricted/Critical, shell.exec tool violation), AGT-003 HR Policy Assistant (sanctioned/Medium).
- Verified: testing_agent iteration_88 — all 7 tabs render/switch/persist, register flow, guardrail toggle→PATCH 200, red-team→POST 200 (HEURISTIC BASELINE label), Executive Brief→POST /studio/report/pdf 200 blob download, empty states for ai-systems/incidents, workflows populated, SAP UAC regressions intact. retest_needed=false.
- Rebrand to match build: login page headline "Agentic AI Security Control Plane" + updated subtext/tag chips (Machine Authority · Guardrail Coverage · Evidence-backed AI); index.html <title>, manifest name/description, and AppShell header org fallback all set to "Obserra — Agentic AI Security Control Plane". Obserra eye brand mark retained.


## 2026-06 — 5 live features backend-wired + SAP scrub (iteration_90 — backend 10/10, frontend E2E pass)
Completed the backend for 5 features whose frontend was written but never wired (they were 404/500 at runtime):
- **Agent Runtime Webhook**: `GET/PUT /api/agents/runtime/webhook` (admin PUT; validates http(s), 400 otherwise; stored on `org.agent_runtime_webhook`). Powers `RuntimeConnectorCard` in Settings (Connected / Not connected pill). Kill-switch enforcement is POSTed here as `{agent_ref, action, mode, org_id}` when set.
- **Neutralise all (Toxicity Map)**: `POST /api/agents/runtime/enforce-bulk` `{action:'suspend'|'kill', selector:'toxic'|'all', refs:[...]}`. Frontend passes the exact toxic refs it renders so the enforced count matches the UI (model.toxic=2 → count 2). Button now offers BOTH **Suspend all** and **Kill all** (user chose "ability to do all"); default one-tap is Suspend (reversible).
- **Advisor Enforce Actions**: added `enforce_from_advisor()` (was imported by `routes.py /actions/run` but never defined → advisor Suspend/Kill 500'd). Refactored the single `enforce_agent` endpoint + bulk + advisor onto one shared `_do_enforce(org_id, actor, ref, action, source)` (flips status, dispatches webhook, writes Defensibility Ledger, Slack/Teams alert).
- **Toxicity Heatmap** & **Live Shadow AI Discovery**: already complete from the prior batch — re-verified (heatmap grid renders; `/api/ai-systems/discover` returns LIVE-only signals, no mock seeds).
- **SAP scrub**: removed visible "SAP UAC" branding across the UI — `Settings.jsx` (deployment/docs copy + guided-tour helper + download filenames → "Obserra-Agentic-AI-Security-*"), `ShareDigest.jsx` brand pill → "OBSERRA", `App.js` comment, `UpdateBanner.jsx` banner. Left factual SAP-data labels intact (ShareDigest genuinely shows SAP access data — relabeling would mislabel live data, violating the "all live" directive).

## 2026-06 — Enforcement observability enhancements (iteration_91 — frontend E2E ~95%, no functional bugs)
Four follow-ups on top of the 5 live features, all verified:
- **Runtime Test Ping**: `POST /api/agents/runtime/webhook/test` sends a synthetic `obserra.runtime.test` event to the saved webhook and returns `{ok,status_code,latency_ms,response}`. `RuntimeConnectorCard` shows a **Send test event** button (only when Connected) + a result line (`runtime-webhook-test-result`). Manually verified end-to-end: click fires the POST and renders result/toast (success path 200/544ms via curl; timeout path "No response — unreachable" also renders). 400 when no webhook saved.
- **Kill Audit Trail**: `GET /api/agents/runtime/enforcement-log` (last 50). Every `_do_enforce` now inserts into `db.agent_enforcements` (ref, name, action/verb, mode, source [manual/advisor/bulk-neutralise], by, runtime, external_ok, receipt, at). `ToxicityMap` renders a **Runtime enforcement activity** feed (`toxicity-enforcement-feed` → `enforcement-row-N`), refreshed after Neutralise.
- **Discovery Alerts**: `POST /api/ai-systems/discover` now fires a Slack/Teams `_post_chat_alert` + in-app `notifications.create` when `added>0` (deduped). No mock data.
- **Enforcement Receipt**: `_do_enforce` captures the webhook HTTP receipt (status_code, latency_ms, response snippet / error, timestamp) onto `agent.enforcement.receipt`. Surfaced in `AgentDetailModal` (`agent-enforcement-receipt`) and as a facet in `agentDeepDive`. Auditors can prove a kill actually reached the runtime.
- **Cleanup**: removed dead `components/SapAdvisor.jsx` (unused since AppShell moved to `AIAdvisor`). NOTE: the broader `Sap*` page/component rename was intentionally NOT done — those files still back the (hidden) legacy SAP routes, so a ~30-file rename is high-risk churn on non-visible code with zero UX benefit; deferred as an isolated pass if wanted.
- Non-bug note from tester: the feed row tag ("runtime ✓") and the modal header ("runtime dispatch failed") can differ — they correctly reflect two *different* enforcement attempts with different network outcomes (httpbin is transiently flaky from the sandbox), not a code inconsistency.
- Verified: backend curl (test 200/544ms, receipt captured, enforcement-log, 400 no-webhook, discovery added 2 live) + testing_agent iteration_91 frontend E2E. Baseline restored (AGT-001/003 sanctioned, AGT-002 restricted, webhook cleared).

## 2026-06 — Evidence & signing round (iteration_92 — frontend E2E 90%→resolved, no functional bugs)
Delivered all 4 requested items + the Evidence Pack, all verified:
- **Enforcement Export**: `GET /api/agents/runtime/enforcement-log/export?format=csv|pdf` — CSV (full columns incl. http_status/latency/attempts/signed) and a branded board/auditor PDF (reuses `reports._build_pdf`, cover page). Export CSV / PDF buttons added to the Toxicity Map enforcement feed. Verified 200 + correct content-types.
- **Webhook Retry + unreachable flag**: `_dispatch_webhook()` now retries up to 3× with backoff; `_do_enforce` sets `runtime_unreachable` on the agent + event when a non-resume dispatch never lands. Feed rows show a red "⚠ never reached runtime (N tries)" tag.
- **Signed Webhooks (HMAC)**: org `agent_runtime_webhook_secret`; `_dispatch_webhook` signs the raw body with HMAC-SHA256 → `X-Obserra-Signature: sha256=<hex>` + `X-Obserra-Timestamp`. Settings card gained a secret input + a "Signed" badge (`runtime-webhook-signed`); GET returns `secret_set` (never the plaintext). Verified: receipt shows `signed:true`.
- **Evidence Pack**: `GET /api/agents/runtime/evidence-pack.pdf` — one-tap board-grade PDF bundling an attestation, live AI-agent toxicity snapshot, and the full enforcement audit trail with receipts. Admin-only button on the Toxicity Map. PDFs visually verified (branded cover, exec summary, per-agent toxicity, receipts).
- **Sap rename (isolated pass)**: renamed the 5 legacy frontend files + all refs (`SapInsight→AIInsight`, `SapAIFix→AIFix`, `SapOverview→AccessOverview`, `SapAnalytics→AccessAnalytics`, `SapSystems→ConnectorHealth`); App.js + routePrefetch updated. Zero stale refs; app compiles; renamed legacy routes (/app, /app/systems, /app/analytics) still mount (no ChunkLoadError). No user-facing SAP data labels changed.
- **Deep-dive receipt testability**: added per-facet `data-testid="deep-dive-facet-<label>"` to `RiskDetailModal` so the "Runtime receipt" facet (surfaced via `agentDeepDive`) is discoverable, complementing the `agent-enforcement-receipt` in the agentic `AgentDetailModal`.
- Notes: the iteration_92 "missing testid" HIGH was a modal mismatch (tester opened `/app/agents` = generic RiskDetailModal, not the agentic AgentDetailModal whose testids are intact at lines 183/195). Fixed `notifications` import in the discovery alert (`from kernel import notifications`). Backend curl-verified; baseline restored (AGT-001/003 sanctioned, AGT-002 restricted; webhook + secret cleared). Pre-existing legacy `<span> in <option>` warning on ConnectorHealth left as-is (hidden legacy page).
- Verified: backend 10/10 pytest (`tests/test_iter90_agentic_v1_wiring.py`) + curl (webhook set/get/clear/400, bulk suspend & kill via refs, advisor suspend/resume 200); testing_agent iteration_90 frontend E2E all 5 flows pass. Demo baseline restored (AGT-001/003 sanctioned, AGT-002 restricted). Note: Settings.jsx line-554 scrub required a re-apply after a silent same-file revert (a recurring quirk on this repo).

## 2026-06 — Unification finish: Auditor Room UI + standardized deep-dive cards (iteration_93 — backend 3/3 pytest, frontend 100%)
Completed the final unification phase (last working item from the fork). No mocks — all live.
- **Read-only Auditor Room UI** (`components/agentic-ai/DefensibilityDashboard.jsx` → new `AuditorRoomCard`, admin-only): one-tap **Create auditor room** → generates an expiring, no-login shareable link (copied to clipboard), lists active rooms with expiry + open-count, and per-room **Copy** / **Revoke**. Backs onto the existing public portal `pages/AuditRoom.jsx` (route `/audit-room/:token`).
- **New backend endpoints** (`agents.py`): `GET /api/agents/runtime/evidence-rooms` (list org rooms w/ url/expiry/opens/expired) and `POST /api/agents/runtime/evidence-room/revoke` (`{token}`; 404 if not found). Complements the existing create (`POST /api/agents/runtime/evidence-room` `{days}`) + public `GET /api/agents/public/evidence-room/{token}` + `/pack.pdf` (signed 217KB PDF).
- **Standardized deep-dive across the last two surfaces** (user chose "keep both"): `AgentInventoryDashboard` rows (`inventory-agent-<REF>`) and `MissionControlDashboard` top-agent cards (`mc-agent-<REF>`) now open the SAME universal `RiskDetailModal` deep-dive (rating/score, compliance chips, AI brief, recommendations, and **Standard actions** Suspend/Kill/Resume via `POST /api/actions/run`). The bespoke `AgentDetailModal` is intentionally kept on **Authority & Tools** and **Guardrails & Red Team** tabs where it adds extra detail (red-team evidence, guardrail toggles, enforcement receipt).
- Fixed a missing `onReload={reload}` on the Shadow AI dashboard call site so AI-system Sanction/Block from the standard deep-dive refreshes the estate.
- Added per-row testids `authority-register-<REF>` + `guardrails-agent-<REF>` (tester ergonomics).
- Connectors already unified in a prior batch (SAP Landscape scope merged into `connectors_catalog.py`); re-confirmed this session.
- Verified: testing_agent iteration_93 — backend pytest 3/3 (`tests/test_iter93_auditor_room.py`: create→list→public JSON+PDF→invalid-token 404/410→revoke→gone; agent suspend/resume + aisys sanction 200) and frontend 100% of flows (auditor room create/copy/revoke, public portal + PDF + invalid-token error, standard deep-dive + live Suspend on Inventory & Mission Control, Shadow AI Sanction/Block, Toxicity Map standard deep-dive; bespoke AgentDetailModal preserved on Authority/Guardrails). Live owner org baseline restored (AGT-001 sanctioned, 0 auditor rooms outstanding). Frontend compiles clean.

## 2026-06 — Auditor Room enhancement pack: notes, watermark, renew, board digest, runtime clause (iteration_94 — frontend 100%, all live)
Five user-requested follow-ups, all E2E-verified (backend curl + testing_agent frontend):
- **Auditor Notes**: public `POST /api/agents/public/evidence-room/{token}/comment` (+ public `GET .../comments` that HIDES `author_email`) → admin inbox `GET /api/agents/runtime/evidence-room-comments` + reply `POST .../evidence-room-comments/reply` (marks Resolved, emails the auditor if they left an email). Public portal (`AuditRoom.jsx`) got a question form + thread (shows governance replies); Defensibility tab got an `AuditorQuestionsCard` inbox with inline reply. In-app + email notify admins/execs on new questions (`_notify_org_staff`).
- **Runtime Kill Connectors**: confirmed Kill/Suspend already dispatch to the real HMAC-signed, retried `org.agent_runtime_webhook` via `_do_enforce` (built in iter 90/92). The standard-action result banner (`RiskDetailModal` StandardActions) now appends the runtime outcome — "· dispatched to the agent runtime ✓" / "· ⚠ never reached the agent runtime" — ONLY when a webhook is connected (control-plane case left to the base message to avoid the duplicate clause the tester flagged).
- **Evidence Watermark**: `public_evidence_room_pdf` now accepts `?who=` and stamps the PDF "Downloaded by <name> · <UTC> · link expires <date>" via `deploy._watermark_pdf`; download count + last-downloaded-by tracked. pymupdf-verified the watermark text renders (5-page PDF).
- **Room Expiry Reminders**: per-room **Renew** button + `POST /api/agents/runtime/evidence-room/renew` (extends expiry, clears reminder flag). `_run_agent_room_expiry_reminders` (email + in-app, deduped) folded into the daily-drift-digest cron.
- **Board Evidence Digest**: **Email board digest** button + `POST /api/agents/runtime/board-evidence-digest/send` (admin, on-demand) → `_run_board_evidence_digest` emails admins/execs a 30-day rollup (kills/suspends/resumes + current toxic agents + sanctioned/shadow AI systems) with the signed Evidence Pack PDF attached (base64, reuses `_evidence_snapshot`/`_evidence_markdown`/`_build_pdf`). Monthly auto-send (skips orgs with nothing to report) folded into `monthly-board-report` cron.
- Verified: backend curl — comment→inbox→reply→Resolved (email hidden from public), renew→new expiry, watermark PDF 230KB with `?who=` (pymupdf-confirmed text), board digest `{sent:1}`, cleanup revoke→0 rooms. testing_agent iteration_94 frontend 100% of all 5 flows, no functional bugs (one cosmetic duplicate-clause nit fixed post-run). Live owner org baseline restored (AGT-001 sanctioned, 0 rooms). Frontend compiles clean.