# Obserra EIOS — PRD

## Status (Jun 2026) — Production-readiness verified + app-wide rich-card sweep (iteration_98)
Live-connection audit PASSED: backend (health 200) + MongoDB (RUNNING) + AI/Emergent LLM (live — `advisor/explain` returned real anthropic/claude-sonnet-5 in ~8.7s; `sap/insight` live; graceful deterministic fallback only past the 15s hard timeout) + correlation engine (`/api/risk-engine` computes live scan_score/ALE/compliance) + connector framework (`/api/connectors/health` reports honest `credentials_required` states). Deployment: was FAIL on `.gitignore` ignoring `.env` → FIXED (lines commented, `git check-ignore` confirms tracked); added `load_dotenv(..., override=False)` hardening; deployment agent now READY FOR DEPLOYMENT. Rich-card sweep: Agentic AI product fully consistent — Defensibility Auditor Rooms now carry a readiness rating+score deep-dive (AI recommendations/fixes), Authority Tool/Permission cards open the rich agent modal; rest of app already opens rich detail modals (verified via audit). ⚠️ CAVEATS (HIGHLIGHT): enterprise connectors (M365/Azure/AWS…) show `live:false / credentials_required` — need the customer's real OAuth creds to ingest live data (not a mock, just unconnected); broader EIOS platform datasets (identities/SoD/SAP) are seeded demo labeled `MOCKED_LIVE` (pre-existing platform demo) — the Agentic AI product itself is fully live.

## Status (Jun 2026) — Agentic AI Security Control Plane v1: DEFENSIBILITY COMPLETE + reporting layer refactored (iteration_96)
Latest (fork continuation): the final 4-feature Defensibility batch is validated — Board **Digest Preview** (`/board-evidence-digest/preview` + `.pdf`), **Auditor Room Access Log** (`/evidence-room/{token}/access-log`, chain of custody), **Auditor-question SLA + second-approver escalation** (governance settings + daily `_run_auditor_question_escalation` cron), and **Runtime Playbook Test Ping** (`POST /runtime/webhook/test`, signed+retried, no-mock). Backend curl-verified + testing_agent frontend 100% (5/5, `iteration_96.json`). Also refactored: `agents.py` 1493→1277 lines, reporting/PDF builders extracted into `backend/agent_reports.py` (236 lines, one-way dep, backend boots clean, all report endpoints re-verified 200 + valid PDF).

## Status (Jun 2026) — Agentic AI Security Control Plane v1: UNIFICATION COMPLETE
The v1 pivot + all Enforcement Observability features + the final unification phase are done and E2E-verified (iteration_93). Standard universal deep-dive cards (agent Suspend/Kill/Resume, AI-system Sanction/Block via `/api/actions/run`) run across Executive Overview, Agent Inventory, Mission Control, Shadow AI and the Toxicity Map; the bespoke `AgentDetailModal` is kept on Authority & Guardrails for extra detail. The read-only, expiring **Auditor Room** (create/list/copy/revoke UI in the Defensibility tab → public `/audit-room/:token` portal + signed PDF) is live. Connectors are unified (SAP Landscape merged into `connectors_catalog.py`). All live — no placeholders/mocks. Owner/admin test account: `jblan2026@gmail.com` / `Obserra2026!`.

### Auditor Room enhancement pack (iteration_94 — frontend 100%, all live)
Five follow-ups, all E2E-verified: (1) **Auditor Notes** — external auditors post read-only questions on the public portal → admin inbox in the Defensibility tab → reply flows back to the portal + emails the auditor (public GET hides `author_email`); (2) **Runtime Kill Connectors** — Kill/Suspend already dispatch to a real HMAC-signed, retried agent-runtime webhook via `_do_enforce`; the standard-action result banner now surfaces the runtime outcome ("dispatched to the agent runtime ✓ / ⚠ never reached") when a webhook is connected; (3) **Evidence Watermark** — the Auditor Room PDF is stamped "Downloaded by <name> · <UTC> · link expires …" via `deploy._watermark_pdf` (`?who=`), download tracked; (4) **Room Expiry Reminders** — per-room **Renew** button + `POST /api/agents/runtime/evidence-room/renew`; reminder emails folded into the daily cron (`_run_agent_room_expiry_reminders`); (5) **Board Evidence Digest** — one-click "Email board digest" (`POST /api/agents/runtime/board-evidence-digest/send`) emails admins/execs a 30-day rollup of kills/suspends/toxic + sanctioned/shadow AI systems with the signed Evidence Pack PDF attached; monthly auto-send folded into `monthly-board-report` cron (`_run_board_evidence_digest`).

### Defensibility power-pack (iteration_95 — frontend 100%, all live)
Five more, all E2E-verified: (1) **Digest Scheduling** — Governance settings card: admin picks board-digest day-of-month (1-28) + recipients (blank = admins/execs) + enable toggle (`GET/PUT /api/agents/runtime/governance-settings`); scheduled send moved to the daily cron, self-gated per org to its chosen day (deduped per month). (2) **Question SLA** — org-configurable SLA hours; inbox shows `SLA {n}h` + `overdue` badges; `_run_auditor_question_sla_nudge` (daily cron) emails admins about questions open past SLA. (3) **Watermark Logo+QR** — the Auditor Room PDF now stamps each page with the org logo + a QR back to the live room + the downloader stamp via `_brand_watermark_pdf` (reportlab QrCodeWidget + `_resolve_brand`). (4) **Runtime Playbooks** — `GET /api/agents/runtime/webhook/playbooks` returns the signed webhook contract + per-provider adapter templates (Generic, LangGraph, CrewAI, AWS Bedrock Agents); rendered as a copyable reference card. (5) **Two-way audit workspace** — threaded auditor Q&A (`messages[]`), auditor public follow-ups (`POST .../comment/{id}/message`), admin replies that can attach the signed evidence PDF, resolve/reopen (`POST .../evidence-room-comments/status`), admin identity hidden publicly; the full Q&A trail is appended (`_qa_markdown`) into the board-digest PDF. All backend curl-verified + testing_agent iteration_95 frontend 100% (15/15); live owner-org baseline restored.

## Product
Enterprise SaaS "continuous AI control plane" with Dual-Mode dashboards (Executive $-impact vs Operational counts), Shared Cybersecurity Kernel, live connectors (M365/Copilot/ChatGPT/Teams/SSO), Compliance Posture, Web-Push, JIT provisioning, installable PWA. React + Tailwind / FastAPI + MongoDB. English only.

## Official Branding (source of truth — the ONLY logo used anywhere)
- User-provided official assets (transparent so they blend on any background):
  - `/public/brand-mark.png` — eye + keyhole symbol
  - `/public/brand-wordmark.png` — "OBSERRA / Executive Protection & Intelligence LLC" (cropped from the official lockup so the font matches exactly)
  - `/public/brand-lockup.png` — horizontal lockup (sidebar/header)
- Generated from official mark: PWA icons, maskable icons (52% safe zone), favicons, 21 iOS splash PNGs (portrait + landscape iPad). Script: `/app/scripts/gen_brand.py`.
- Used on: Auth (symbol + large wordmark), sidebar, header, advisor, install banner, splash, favicon, onboarding tour badge. No third-party logos except required Google/Apple sign-in button marks.

## Board Report PDFs (reports.py `_build_pdf`)
- Branded on every export (board report, studio report, evidence pack): top-left badge, subtle centered watermark, footer "Obserra — Executive Protection & Intelligence LLC · Confidential".
- Board report (`POST /api/reports/pdf`, cover=True) adds:
  - Branded **cover page** (navy + full lockup + title + org name + date + confidential banner).
  - **Portfolio Residual Exposure ($M) line chart** (reportlab graphics) from health history + residual ALE.
  - Auto **Key Takeaways & Recommended Actions** derived from metrics (exposure/reduction %, critical risks, pending recs).
- Backend brand assets: `/app/backend/assets/` (brand-badge.png, brand-watermark.png, brand-lockup.png).
- Email HTML report also carries the logo (public asset URL).

## Onboarding Tour (OnboardingTour.jsx, mounted in AppShell)
- Role-aware: Executive (admin) vs Operational steps. 3 steps, progress bar + dots, Back/Next/Skip/Get started.
- Only the official mark shown in the step badge.
- Spotlight: dims screen + pulses ring around real `[data-testid=mode-toggle]` on mode steps.
- a11y: role=dialog, aria-modal, aria-labelledby, Escape closes; backdrop click dismisses WITHOUT persisting done flag.
- Persist per-user (`obserra-tour-done-<id>`); Replay from Settings ("Replay tour", `obserra-replay-tour` event).

## Test status
- iteration_14.json: frontend 6/6 PASS (brand consistency + exec/ops tour + spotlight + replay).
- Board PDF verified via render (doc-verification skill): cover + chart + takeaways + watermark/footer all correct.
- Credentials in `/app/memory/test_credentials.md`.

## Recent polish (Jun 2026)
- Settings: "Remove logo / Reset to Obserra" button; logo type/size validation + dropped-invalid-email recipient feedback.
- AIAdvisor floating button: navy (#0f1e3d) pill, white eye mark; first-time hint bubble + pulse (hint-open analytics in /advisor/usage); in-chat reply avatars use navy eye badge.
- Report Branding: LIVE cover preview (pymupdf GET /api/reports/branding/preview) with Dark/Light toggle; brand accent colour flows into cover org text + trend line + risk bars.
- Board Report modal: branded cover thumbnail preview column (theme-aware).
- **Distribution & docs**: PWA hardened for one-click install (service worker /push-sw.js with offline shell registered on load; manifest id/shortcuts) across desktop/mobile. On-premise Docker package (/app/deploy/onprem: docker-compose, Dockerfiles, nginx.conf, .env.example, INSTALL.md, optional install.sh) downloadable via GET /api/deploy/onprem-package (admin). Auto-generated PDF + Word Install & User Guide with dashboard screenshots (scripts/gen_docs.py → /api/deploy/guide.pdf, /guide.docx, admin). Settings "Deployment & Documentation" card exposes all three downloads. Video walkthrough NOT produced (agent cannot record video) — built-in Guided Tour offered as in-app alternative. Verified iteration_19 (backend 9/9, frontend 100%).

## Backlog / Roadmap
- ✅ DONE (Jun 2026) — Verification watermark on shared/audit PDFs (pypdf), digest test recipient (`POST /api/deploy/health-digest-test-email`), Share Link Manager (`GET /api/deploy/evidence/shares` + revoke), Scheduled Quarterly Pack (`quarterly_pack` toggle, folded into monthly cron), and public Audit Room portal (`GET /api/deploy/audit-room/{token}` + watermarked `/evidence`, with live findings). Backend curl-verified + testing_agent frontend 7/7 (iteration_78).
- ✅ DONE (Jun 2026) — Evidence retention (`keep` in evidence-config), custom report period (month/quarter with period uptime), auditor share links (public `GET /api/deploy/evidence/shared/{token}`), QR verification (public `GET /api/deploy/evidence/verify/{token}` + `db.evidence_verify` SHA-256 binding), and on-demand digest send (`POST /api/deploy/health-digest-send`). QR via reportlab built-in (no new dep). Backend curl-verified + testing_agent frontend 7/7 (iteration_77).
- ✅ DONE (Jun 2026) — Evidence Locker + monthly evidence email + restore-preview log + digest preview + tour auto-skip: `POST /api/deploy/evidence/generate`, `GET /api/deploy/evidence/list|download`, `GET/PUT /api/deploy/evidence-config` (monthly email folded into the `monthly-board-report` cron, no new cron), `GET /api/deploy/restore-previews` (dry-run audit trail), `GET /api/deploy/health-digest-preview`, and `OnboardingTour` no longer auto-opens over `/app/system-health`. Backend curl-verified + testing_agent frontend 9/9 (iteration_76).
- ✅ DONE (Jun 2026) — System Health compliance suite: **Passphrase Rotation** (`POST /api/deploy/backup-encryption/rotate`, re-encrypts existing snapshots), **Restore Preview** (`POST /api/deploy/restore-preview`, non-destructive per-collection count diff), **Compliance Evidence PDF** (`GET /api/deploy/compliance-evidence`, reportlab signed PDF), and **Alert Digest** (daily bundled degraded-events digest per channel in `run_health_alerts`). Backend endpoints had been dropped by a silent revert and were re-added this session; frontend UI in `SystemHealth.jsx` was already built. Backend curl-verified end-to-end (incl. encrypted 400/403/200 security path + valid PDF) and testing_agent frontend 100% (iteration_75).
- Filters + weekly cron + two-pane everywhere + recipients book (Jun 2026): search+status/role/tier filters on Team/Vendors/Agents (apply to cards AND tables via shown* arrays); weekly guide-refresh cron (`POST /api/cron/weekly-guide-refresh`, WEBHOOK_CRON_SECRET) — capture offloaded to a DETACHED subprocess so it never blocks the API loop; in crons.yml top-5 (weekly-studio-report bumped below the active-5 line — flagged to user); two-pane detail for Vendors (`vendor-detail-pane`) and Control Monitoring (`control-detail-pane`); IT recipients book (`GET/PUT /api/deploy/recipients`) with Settings chips (save/pick/remove). Verified iteration_22 (backend 100%, frontend 100%; tester fixed 3 crashers: AIAgents missing q/statusF state, Settings missing Bookmark/X imports, Team members.map→shownMembers.map).
- Card views + pipeline + two-pane + email (Jun 2026): mobile card views for Team (`member-cards-mobile`), Third-Party Risk (`vendor-cards-mobile`), AI Agents (`agent-cards-mobile`) with desktop tables `hidden md:block`; Playwright screenshot pipeline `scripts/capture_shots.py` wired to `POST /api/deploy/regenerate-guides?capture=true`; Risk Register tablet/desktop two-pane detail (`risk-detail-pane`, row click sets `selected`, mobile keeps modal); admin email-docs (`POST /api/deploy/email-docs`, PDF+zip attachments via kernel.notifications) with Settings email UI. Verified iteration_21 (backend 7/7, frontend 100%).
- Tablet + mobile UX pass (Jun 2026): tablet 2-up breakpoints (Overview exec+operational KPIs `md:col-span-2`, operational quarter charts 2-up); Advisor first-time hint anchored just above the FAB and clamped to viewport width; Risk Register filter bar sticky (`top-16`); mobile card views for data-heavy tables (Risk Register `risk-cards-mobile`, Control Monitoring `control-cards-mobile`) with desktop tables `hidden md:block`; guide auto-refresh (POST /api/deploy/regenerate-guides admin + startup regen + Settings "Regenerate guides" button). Verified iteration_20 (backend 5/5, frontend 100%).
- Mobile responsiveness pass (Jun 2026): global `overflow-x-hidden` + `min-w-0` on main content with mobile bottom padding (clears floating Advisor); Enterprise/tab rows made horizontally scrollable; Situation Room rows use min-w-0/truncate + shrink-0 badges; Kernel KPI + Evidence modal grids made responsive. Verified 0 horizontal overflow (scrollWidth==clientWidth at 390px) across Enterprise, Situation Room, Kernel, Controls, Risk Register, AI Governance.
- P2 polish — landscape splash, nav spotlight, auth toggle, report cover (Jun 2026, iteration_27 — frontend 100%):
  - **Landscape iPhone Splash**: gen_brand.py now emits portrait+landscape for all 15 devices (30 apple-touch-startup-image tags in index.html).
  - **Role Nav Spotlight**: OnboardingTour steps support a `target` testid; exec tour spotlights `nav-overview`, ops tour spotlights `nav-risk-register`.
  - **Auth Button Toggle**: admin switch (Settings `login-screen-settings` → `PUT /api/settings/auth-ui`, global app_config) hides Google & Apple on the login screen; `/api/auth/providers` returns `hide_social`.
  - **Report Cover Options**: Board Report modal `cover-options` bar (date + version/revision) threaded through `/api/reports/pdf` → `_paint_cover`/deck footer. Cover PDF visually verified.
- PAID DASHBOARD ADD-ONS (Jun 2026 — backend verified via curl + grant sim; extends existing payments.py/Marketplace):
  - **6 packs** (each = one entitlement, monthly+yearly Stripe prices, placeholder pricing): AI Governance ($79/$790), Cyber Risk ($99/$990), Third-Party Risk ($59/$590), Asset Intelligence ($49/$490), Audit & Evidence ($39/$390), Reporting & Board ($69/$690). Enterprise all-access ($2999/mo, $32388/yr) unlocks everything (plan="enterprise").
  - Backend `payments.py`: CATALOG + PACKS + `_PACK_BY_LOOKUP`; `/api/modules` returns packs w/ owned; `/api/modules/checkout` accepts `lookup_keys[]` (multi-line subscription checkout); `_grant` grants entitlement + issues license key (`OBS-XXXX-…`, stored on `org.licenses`) + emails admins; `/api/licenses` lists keys. Stripe sandbox catalog created via setup_catalog.
  - Frontend: Marketplace rewritten (6 pack cards, monthly/yearly toggle, multi-select + "Enable access" bar → multi checkout); `LockedGate.jsx` upgrade screen (per-pack pages + monthly/yearly buy buttons) shown by AppShell when a route's `ent` isn't owned (NAV items now carry pack entitlements); Settings "Add-on License Keys" card (copyable). PaymentSuccess polls status → grant.
  - Tax mode: Stripe-managed (SMP) with automatic-tax fallback (US sandbox).
  - VERIFICATION NOTE: gating UI (LockedGate) not visually shown on the demo org because it is Enterprise/all-access; a trial or per-pack org sees gates. Full Stripe checkout completion needs a real test-card purchase (external Stripe page).
  - **Momentum Digest (email)**: `_run_momentum_digest` wired into the already-scheduled `weekly-drift-digest` cron (Mon 08:00 UTC) — emails admins/execs a one-liner "Risk-reduction score moved X→Y this week" + counts, via `compute_momentum(org_id)` (refactored shared helper in routes.py). Verified: cron 200, notification "score 0 to 66 emailed".
  - **Bulk Log Export**: admin `GET /api/reports/logs-pack.pdf` — one branded "Audit Evidence Binder" PDF (cover page + all control & vendor logs grouped by ref/owner). Settings card `evidence-binder-settings` with download button. PDF layout visually verified.
  - **Owner Auto-Match**: `GET /api/owners` now returns a `suggestion` per owner (exact then first-name match against team members); Settings shows a "Use <email>" button (`owner-suggest-*`) to one-tap fill. Verified: Dana Ops → analyst@obserra.demo.
- Momentum trend + owner directory + log export (Jun 2026, iteration_25 — backend 9/9, frontend 100%):
  - **Momentum Trend**: `/api/remediation/activity` returns `trend[]` (8 weekly points, each a 30-day trailing score); Executive "Remediation Momentum" card renders an area chart (`exec-momentum-trend`) of the trajectory.
  - **Owner Directory**: `GET/PUT /api/owners` (org.owner_directory, name→email, lowercased keys). `_nudge_owner` resolves directory → user-name match → admin fallback. Settings "Owner Directory" card (`owner-directory-settings`) lists distinct control+vendor owners with editable emails. Vendors now carry an `owner` field (seeded per-vendor; existing backfilled to "GRC Team").
  - **Log Export**: admin-only branded PDFs `GET /api/reports/control-log/{id}.pdf` & `/api/reports/vendor-log/{ref}.pdf` (reports.py `_log_pdf`, reuses `_build_pdf`/brand). Detail-pane "Export PDF" buttons (`control-log-export`/`vendor-log-export`) shown for admins when the log is non-empty. PDF layout visually verified (badge, title, owner subtitle, per-entry kind/date/text/author, watermark, footer).
- Remediation log polish — filtering, owner nudges, exec rollup (Jun 2026, iteration_24 — backend 6/6, frontend 100%):
  - **Log Filtering**: Control & Vendor detail-pane logs are filterable by kind (`control-log-kind`/`vendor-log-kind`) and searchable (`control-log-search`/`vendor-log-search`); empty result shows "No matching entries." (client-side `shownHistory`).
  - **Owner Notifications**: Adding a `remediation` note fires `_nudge_owner` (routes.py) — creates an in-app notification and emails the matched owner (by name→user) or falls back to org admins/execs; vendors nudge admins/execs. Non-remediation kinds do NOT notify.
  - **Exec Log Rollup**: `GET /api/remediation/activity` returns a risk-reduction momentum score (`min(100, remediation*12 + evidence*6 + applied_recs*8)`, 30-day window) + recent merged activity. Executive Overview renders a "Remediation Momentum" card (`exec-remediation-momentum`, `exec-risk-reduction-score`, `exec-remediation-activity`), auto-refreshing every 20s.
- Shareable Filters + Recipients One-Tap Send + Two-Pane Deep Actions (Jun 2026, iteration_23 — backend 8/8, frontend 5/5, 100%):
  - **Shareable Filters**: `useUrlState` hook (URL query sync via useSearchParams, replace:true) applied to search/status/category/role/tier filters on Cyber Risk Register (`/app/risks`), Control Monitoring, AI Agents, Team, Third-Party Risk. Filtered views are bookmarkable/shareable; state restored on reload & direct URL.
  - **Recipients One-Tap Send**: `POST /api/deploy/email-docs-all` blasts PDF guide + on-prem zip to all saved `deploy_recipients` (400 when none; swallows per-recipient failures, returns success count). Settings "Deployment & Documentation" card shows "Send to whole IT list (N)" button when the book has entries.
  - **Two-Pane Deep Actions**: Control (`control-detail-pane`) & Vendor (`vendor-detail-pane`) detail panes now embed an inline "Remediation & evidence log" (add + read). Backend: `GET/POST /api/controls/{id}/history|notes` (control_notes) and `GET/POST /api/vendors/{ref}/history|notes` (vendor_notes); kind whitelist remediation/evidence/note, empty text → 400.
- P1 (blocked): Apple Sign-In + Enterprise SSO — awaiting user IdP metadata/Apple keys (wired, gated by `/api/auth/providers`).
- P2: landscape iPhone splash; per-role nav spotlight; hide Google/Apple buttons if user wants zero 3rd-party logos; report cover date/version options.


## Security Scanner + AI Autonomous Remediation + ECG Vitals (Aug 2026, iteration_33 — backend 12/12, frontend 100%)
- **Live pen-test scanner** (`self_scan.py`): probes the REAL public endpoint (`_target_base()` → FRONTEND_URL) through the ingress (not localhost), checks security headers, CORS, and dependency CVEs live via OSV.dev + CISA KEV cross-reference. Score + severity breakdown; results auto-update the compliance crosswalk (`scan_evidence` gaps/aligned).
- **Both MITRE datasets** on every finding: MITRE ATT&CK techniques (`_MITRE`, `mitre[]` + `mitre_techniques[]`) AND MITRE CWE weaknesses (`_CWE`, `cwe[]` + `cwe_ids[]`).
- **AI Autonomous Remediation Engine** (Emergent LLM key + Claude sonnet, `_ai_review`): daily cadence (fanned out from the already-scheduled `daily-drift-digest` cron via `_run_autonomous_all`), pause/resume + enable/disable + auto-apply-config toggle stored on `org.auto_engine`. Auto-applies ONLY safe non-breaking config fixes (`_AUTO_SAFE_IDS`); dependency upgrades ALWAYS notify + wait for admin approval (`scan_approvals`, in-app notifications). Endpoints: GET/PUT `/api/self-scan/engine`, POST `/engine/run`, POST `/upgrade/approve` {approval_id, approve}.
- **First one-click-install bootstrap** (`bootstrap_first_install`, idempotent via `platform_config.installed_at`, scheduled at startup): records the endpoint, enables the daily engine for every org, and runs an initial live scan so the app goes live with THAT endpoint's data.
- **Connected devices & health** (`GET /api/self-scan/assets`): connector sources + health, live Intune managed-device inventory via Microsoft Graph (`_m365_devices`, needs DeviceManagementManagedDevices.Read.All — shows connect note when M365 not connected), and an `overview` {app_health, compliance_pct, security_score}. Devices labelled by name · owner · os/model.
- **ECG heartbeat UI** (`SecurityScanner.jsx` `HeartbeatTrace`): hospital-monitor vitals — pulse (verified/green), slow-warn (stale/degraded/amber), standby (available/cyan idle-blip), flatline (disconnected/grey), each with a numeric health index. 3 primary monitors (App Health, Compliance, Endpoint Security). Auto-polls every 20s so newly connected sources/devices stream in live. Fixed the iter32 `toggleRemediate` ReferenceError.

## Threat-intel continuous sync + AI Autofix + Real Patch Apply + Trends/Alerts/Device drilldown (Aug 2026, iter34 passed 100%)
- Real Patch Apply: approving a dependency upgrade runs a background maintenance job (pip upgrade → re-pin requirements.txt → auto re-scan) that provably confirms the CVE cleared. Success path proven (certifi no-op), graceful no-fix path (ecdsa) works, and incompatible upgrades (starlette 0.47.2 vs FastAPI 0.115.6) stay gated behind approval. Endpoints: POST /api/self-scan/upgrade/approve returns job_id; GET /api/self-scan/maintenance[/id].
- Scan History Trends: GET /api/self-scan/trend feeds a recharts area/line trajectory (score + open findings).
- Slack/Teams Alerts: org.scan_alerts {teams_url, slack_url} (Teams falls back to live_teams webhook). _post_chat_alert fires on every "approval needed" queue + upgrade outcome. GET/PUT /api/self-scan/alerts (empty fields are no-ops so one webhook never wipes the other), POST /alerts/test.
- Live Device Drilldown: GET/POST /api/self-scan/device/{id}/checklist + POST /device/{id}/sync (Graph syncDevice). Non-compliant Intune devices open a one-click remediation checklist modal. Shows connect-note when M365/Intune not connected.
- Continuous threat-intelligence sync (_sync_intel, 6h TTL + on-scan refresh via _load_kev_set + daily cron + install bootstrap): CISA KEV catalog (version/count/released), MITRE ATT&CK release version (attack-stix-data index), OSV (live-per-scan), MITRE CWE (v4.16). GET /api/self-scan/intel shows per-feed last-updated; POST /api/self-scan/intel/refresh = Force update button.
- AI Autofix now: POST /api/self-scan/autofix runs scan+AI review+apply-safe+queue-upgrades immediately (force bypasses engine enabled/paused). Prominent "AI Autofix now" button next to Run live scan.

## Close-loop upgrades + Feed rules + Exploit timeline + Device auto-remediate + Real-time containment (Aug 2026)
- Real Upgrade Close-Loop: _COUPLED map bumps companion packages together (FastAPI↔Starlette, Pydantic→FastAPI); pins all bumped pkgs to installed versions; _smoke_import subprocess check gates job status (success/applied/failed) so a broken coupled framework never silently ships.
- Feed Alert Rules: _sync_intel diffs new KEV entries (added_since_last) and _alert_new_kev_matches pings Teams/Slack + notifications when a newly-added KEV CVE matches a dependency in the org's latest scan.
- Exploit Timeline: KEV dateAdded stored in threat_intel.kev_set.dates; findings carry kev_added; UI shows "In CISA KEV since <date> · Nd to remediate".
- Device Auto-Remediate: POST /api/self-scan/device/{id}/remediate pushes compliance policy via Graph syncDevice + windowsDefenderScan, completes checklist; modal "Auto-remediate (push policy)" button.
- Real-time Threat Containment: containment_events collection; _evaluate_threats runs on every scan + on new-KEV sync; auto-contains actively-exploited deps (advisory) and M365 high-risk users (real revokeSignInSessions when connected). Endpoints GET /api/self-scan/containment, POST /containment/{id}/review {acknowledge|rollback}, POST /containment/scan. UI: "Real-time threat containment" live feed with enforced/advisory tags + Acknowledge/Roll back.

## AI-driven-first upgrades + boot-check outage gate + manual override (Jun 2026)
- Directive fulfilled: "all is AI driven first, human approval only if a dependency upgrade would cause an outage." The engine now AUTO-APPLIES fixes; humans are only pulled in on a proven potential outage.
- `_run_upgrade_job` (self_scan.py): after the isolated sandbox passes pip-install + coupled-framework smoke-import, it runs a REAL FastAPI boot+health check (`_sandbox_boot_check` — boots a minimal app on uvicorn in the sandbox venv and hits /health). If no outage (respects `org.auto_engine.auto_promote`, default True) it auto-invokes `_promote_upgrade_job` (pin live + re-scan) — no human needed. Any failure (install fail / import crash / boot fail) calls `_flag_for_approval` → job status `requires_approval`, live untouched, + Slack/Teams + in-app alert.
- Autonomous engine (`_run_autonomous`): dependency findings with a fixed_version now AUTO-LAUNCH a sandbox-verified upgrade job (`by="ai-engine"`) instead of queuing a scan_approval. Config fixes still follow the safe-apply / approval path.
- Manual override (with alerts): `auto_promote` toggle on `org.auto_engine` (EngineCfg + GET/PUT `/engine`, defaults True). When OFF, verified upgrades hold at `verified` and alert for manual promotion. Frontend: `engine-autopromote` checkbox + `override-<pkg>` "Override & apply" button on `requires_approval` jobs (with confirm).
- Promote endpoint accepts `verified` OR `requires_approval` (400 otherwise). Containment default playbook is now critical-only auto (identity high → review).
- VERIFIED (Jun 2026): backend boots clean; boot-check returns True on a healthy FastAPI; auto_promote persists via curl; `/autofix` auto-launched a real `starlette` framework upgrade (by=ai-engine) that passed sandbox+boot-check and auto-promoted to `applied` with no human step; promote guard returns 400 (wrong status) / 404 (missing).

## Rollback + Canary + Outage Queue + Upgrade Digest + Autonomy Timeline (Jun 2026)
- Auto-rollback on regression (`_promote_upgrade_job`): captures baseline score + prev installed versions before promoting; after re-scan, if score dropped (or canary fails) it auto-reverts via `_rollback_versions` + re-pins, status `rolled_back`, alerts. Toggle `org.auto_engine.auto_rollback` (default True).
- Canary promote (`_canary_probe`): optional live health-hold window (5×/2s probes of /api/self-scan/ping); widen to live only if every probe holds, else rollback. Toggle `canary_promote` (default False).
- Outage Review Queue (frontend `outage-review-queue`): focused panel of `requires_approval` jobs with the failing sandbox/boot log inline + Override & apply / Dismiss. New POST `/api/self-scan/maintenance/{id}/dismiss` (status `dismissed`).
- Upgrade Digest (`_run_upgrade_digest`, wired into daily-drift cron): daily Slack/Teams + in-app rollup of auto-applied vs rolled-back vs needs-approval jobs (last 24h).
- Autonomy Activity timeline: `autonomy_activity` collection + `_log_activity` hooks (auto-fix, auto-upgrade, auto-applied, rollback, needs-approval, contained, digest). GET `/api/self-scan/activity`. Rendered on Executive dashboard (`exec-autonomy-activity`) so leadership sees fixes shipping live.
- Engine config additions on EngineCfg/GET-PUT `/engine`: `auto_rollback`, `canary_promote` (get_engine setdefaults so existing orgs inherit True/False).
- VERIFIED (Jun 2026): canary/rollback config persists via curl; `/autofix` → real coupled upgrade `starlette 0.49.1→1.3.1 + fastapi→0.141.1` sandbox-verified + boot-checked + auto-promoted, re-scan cleared to score 88; `/activity` returns events; both UIs render (toggles + Autonomy Activity timeline with live events). Frontend compiles clean.

## Safe-restart, rollback history, digest preview, activity filters, board scorecard (Jun 2026)
- Safe restart (`POST /api/self-scan/restart`): one-click detached `supervisorctl restart backend`; on boot `_finalize_applied_jobs()` re-scans and flips `applied`→`success` once CVEs clear. UI: `safe-restart-btn` shown when any job is `applied`.
- Rollback/change history (`GET /api/self-scan/maintenance-history`): per-package history with actor (`promoted_by`/`dismissed_by`/`by`), status, from→to, date. UI: `history-toggle` + `rollback-history` table. Promote endpoint now records `promoted_by`.
- Digest preview (`POST /api/self-scan/digest/preview`, admin): sends the daily remediation digest to chat alerts on demand + returns body. UI: `digest-preview-btn`.
- Activity filters + export (Overview `exec-autonomy-activity`): filter chips (all/upgrades/rollbacks/containment/fixes/approvals) + CSV `Export`.
- Board Autonomy Scorecard: `ai_advisor._autonomy_scorecard` appends a deterministic "## Autonomy Scorecard" section (fixes shipped, outages prevented, auto-rollbacks, threats contained, mean-time-to-patch, 30d) to every generated board report.

## Financial defensibility: auditable math + configurable SLE + external benchmark (Jun 2026)
- Honesty note: the old $ figures came from a hardcoded `SLE_BY_IMPACT` table (analyst assumption), NOT from AI and NOT from org data; per-risk "$X ransomware" strings are static seed labels. This work makes the model auditable, calibratable, and benchmarked.
- `_fin(r, cfg)` now returns full derivation: `sle`, `sle_source`, `aro`, `aro_basis`, `inherent_ale`, `residual_ale`, `confidence`, `risk_adjusted`, and a human-readable `math` string. Used by `/financials`, `/financial/basis`, and `metrics.dashboard` (exec numbers now reflect org config).
- Org `financial_config` (per-org): editable `impact_sle` table, `industry`, `method` (`flat` impact→$ table OR `records` × per-record cost), `records`, `per_record_cost`. Endpoints: `GET/PUT /api/financial/config` (PUT admin-only), `GET /api/financial/basis`, `POST /api/financial/benchmark/refresh`.
- External benchmarks (stored, updated-on-request, no live scrape): `BENCHMARKS` = IBM Cost of a Data Breach 2026 (global $4.99M) + 2025 per-industry table (Healthcare $7.42M … Public $2.86M) + Verizon DBIR medians (ransomware $46k, BEC $50k). `/financial/basis` returns modelled avg/max SLE vs industry benchmark with a ratio.
- UI: CyberRisk `financial-basis` panel — benchmark comparison cards (modelled vs IBM industry vs ratio), DBIR context line + source/updated, per-risk math table with SLE source, and admin calibration (industry select, method toggle, editable impact→$ inputs or records/per-record, refresh benchmark).
- VERIFIED: basis/config/records/reset all via curl; custom table persists & flips source label to "org-configured"; ratio recomputes (1.01× Technology → 1.08× Healthcare); exec dashboard still returns exposure; CyberRisk panel renders. Test calibration reset to defaults after verification.

## Benchmark yearly-refresh, records auto-fill, Board-PDF benchmark, Monte-Carlo ranges, CRO sign-off (Jun 2026)
- Benchmarks now stored in `db.app_benchmarks` (seeded/preloaded from the IBM/DBIR constant). `_maybe_refresh_benchmarks()` best-effort re-fetches at most once/year (wired into daily-drift cron; self-guards on 365d) and on manual `POST /financial/benchmark/refresh`. Verified: live fetch updated global_avg. Explicit per-value source tags added: `industry_avg_source` (IBM 2025 industry table), `global_avg_source` (IBM 2026), `dbir_source` (Verizon DBIR 2025), plus `checked_at`.
- Records auto-fill: `_suggested_records(org)` derives records-at-risk from connected sources (Entra/Tenable/CASB records × 50/identity, MOCKED_LIVE); exposed in `/financial/config.suggested_records`; UI `fin-autofill` button switches to records method and applies.
- Board PDF/deck benchmark: `_board_metrics` computes `bench_line` (modelled vs IBM industry avg + ratio + DBIR median + source) and `signoff_line`; rendered in `_build_pdf` (Financial Benchmark section) and the deck key-metrics slide (caption + CRO sign-off line).
- Scenario ranges (Monte-Carlo): `_montecarlo` 2000-iter simulation → portfolio P10/P50/P90; per-item `ale_low`/`ale_high`. In `/financial/basis`; UI `fin-scenario` band.
- CRO assumption sign-off: `POST /financial/config/signoff` (name) locks calibration with signer+timestamp+config hash; `POST /financial/config/unlock`; PUT config returns 409 while locked; `stale` flag when config changed since sign-off. Shown as approved badge on dashboard + board report/deck. Endpoints admin-only via `require_roles`.
- VERIFIED via curl: sources, suggested_records, scenario P10/P50/P90, per-item bands, signoff lock→409→unlock→stale, benchmark refresh. Both services compile/boot.

## AI-cost benchmark, methodology appendix, per-risk Monte-Carlo, sign-off history, benchmark trend, drift note (Jun 2026)
- AI-cost benchmarks: added `ai_breach_avg` ($6.0M, IBM 2026 AI-enabled breaches) + `shadow_ai_premium` (+$670k, IBM 2025) with explicit sources; `_get_benchmarks` backfills these into existing stored doc. Shown in CyberRisk (`fin-ai-cost` cards) and board `bench_line`.
- Methodology appendix (`ai_advisor._methodology_appendix`): appended to every board report — FAIR model explanation, external citations (IBM 2025/2026 + AI + DBIR with sources/updated), Monte-Carlo note, and CRO sign-off provenance (or "not yet signed"). Verified via direct call.
- Board drift note: same appendix opens with a plain-English "Board Note — Exposure vs Industry" — flags when modelled per-incident runs >1.25× the IBM industry avg (else "in line/defensible").
- Per-risk Monte-Carlo: `_montecarlo_item` gives each risk its own P10/P50/P90 (replaces flat ½×–2×); `/financial/basis` items now carry `ale_low`/`ale_expected`/`ale_high`. UI math table shows the P10–P90 band per risk.
- Sign-off history: `signoff_config`/`unlock_config` push to `financial_config.signoff_history`; `GET /financial/signoff-history` returns who/when/action. UI `fin-signoff-history` audit trail.
- Benchmark trend: `GET /financial/benchmark-trend` derives modelled-exposure-over-time (from health history) vs the flat IBM industry line. UI `fin-trend` Recharts line chart (recharts already a dep).
- VERIFIED via curl: AI-cost fields, per-risk bands (asymmetric), trend points, signoff history entries, methodology appendix text. Both services compile/boot.

## Snapshot trend + per-risk PDF bands + industry auto-detect + sign-off reminders (Jun 2026)
- Snapshot Trend (monthly actuals): `_record_exposure_snapshot(org_id)` upserts monthly modelled avg SLE + residual total + industry benchmark into `db.exposure_snapshots` (called on every `/financial/basis` load and via `_record_all_snapshots` in the daily-drift cron). `GET /financial/benchmark-trend` prefers ≥2 real snapshots (`real:true`) and falls back to health-history derivation. UI `fin-trend` Recharts line (modelled vs IBM avg).
- Per-Risk Bands in Board PDF: `_board_metrics` now returns `risk_bands` (top-6 by residual_ale, each with P10/P50/P90 via `_montecarlo_item`); `_build_pdf` renders a "Per-Risk Scenario Ranges (Monte-Carlo P10/P50/P90)" reportlab Table (Risk · P10 low · P50 expected · P90 high) with a methodology caption. Risk labels show FULL name "CR-004 — <risk title>" (wraps in cell) so boards read the risk, not just the ref. VERIFIED via rendered PDF (document-verification skill).
- Industry Auto-Detect: `_detect_industry(org_id)` matches org sector/name keywords → returns `{industry, reason}`; exposed as `suggested_industry` in `GET /financial/config`. UI `fin-industry-suggest` button ("Use suggested: X") appears only when current industry ≠ suggestion; one-tap applies. Frontend verified 100% (iteration_36).
- Sign-off Reminders: `_signoff_reminders()` (daily-drift cron) scans orgs with a locked calibration whose config hash drifted from the sign-off hash and fires an in-app "calibration stale — re-sign" notification (deduped via `signoff-stale:<hash>`).
- VERIFIED (Jun 2026): backend boots clean; PDF band table renders with full risk names; `/financial/config` returns suggested_industry; frontend Cyber Risk panel + industry-suggest flow passed 100% (test_reports/iteration_36.json).

## FAIR model visualization + FAIR-AIR AI risk reduction + NIST AI RMF mapping + AI-driven synthesis (Jun 2026, iteration_38 — frontend 100%)
- **Chart crash fix**: replaced the React19/Recharts vertical BarChart ("Per-risk uncertainty · P10→P90 band") that fell back to "Chart unavailable" with a pure CSS horizontal band (`fair-waterfall`, `fair-band-<ref>`). Crash-proof; other charts still use `ChartBox` (fair-lec, fin-trend). Removed unused `BarChart`/`Bar`/`LineChart` imports.
- **Quantification pipeline** (`fair-pipeline`): 5-stage flow showing how the cyber model ties together — Signals & evidence → FAIR factors → Monte-Carlo → Aggregate exposure → Board decision, populated with live counts (sources, controls, risks, $ at risk, ROI).
- **FAIR ontology tree** (`fair-ontology`, `FactorNode`): canonical FAIR decomposition Risk (ALE) → LEF + LM → TEF/Vulnerability → Contact Freq/PoA/TCap/RS, and Primary Loss/Secondary Risk → Secondary LEF/LM. Nodes show portfolio-average LEF/TEF/Vulnerability/LM. Source: Open Group FAIR™ (O-RA).
- **FAIR-AIR section** (`fair-approach`): label badge "FAIR-AIR · AI RISK REDUCTION" (`fair-air-label`); guiding quote (`fair-air-quote`, "meet business needs, not create obstacles to AI deployment"); the 5 GenAI risk vectors from the FAIR-AIR playbook (`fair-air-vector-0..4`: Shadow GenAI, Foundational LLM, Hosting on LLMs, Managed LLMs, Active cyber attack); 5-step method (`fair-step-0..4`: Contextualize→Scope→Quantify→Prioritize→Decide).
- **Quantified scenarios** (`fair-air-scenarios`): FAIR-AIR output format "There is a X% probability in the next year that … which will lead to $Y in losses" + key risk driver. Illustrative by default; swaps to AI-generated on demand.
- **NIST AI RMF alignment** (`fair-nist`): GOVERN/MAP/MEASURE/MANAGE core functions mapped to the reasoning. **Controls library** (`fair-nist-controls`, scrollable `fair-controls-list`, 16 controls): each shows NIST ref, TYPE (Governance/Preventive/Detective/Corrective), coverage % + Met/Partial/Gap, the GenAI vector it treats, and compliance frameworks met (NIST AI RMF · ISO/IEC 42001 · SOC 2 · EU AI Act · GDPR). Summary: overall coverage %, controls met, framework count, per-function bars.
- **AI-driven FAIR-AIR analysis** (backend `ai_advisor.py`): `POST /api/advisor/fair-air` (BackgroundTasks) + `GET /api/advisor/fair-air/{job_id}`. `generate_fair_air_analysis()` uses an ADVANCED REASONING system prompt and `_all_dashboards_context()` which synthesizes across EVERY dashboard (FAIR/financial + security scanner + compliance crosswalk + autonomous remediation + threat containment + connectors + threat intel). Returns strict JSON scenarios. Frontend "Analyze with AI" button (`fair-air-analyze-btn`) polls to done and swaps in AI scenarios with an `fair-air-ai-badge` (expected annual AI loss). Verified via curl (5 well-formed scenarios citing CR-001..CR-006, IBM $6.0M AI-breach avg, connector coverage) and iteration_38 frontend (job ~21s, badge + scenario swap + NIST chips).

## 4-feature governance build + Claude→GPT switch + International AI Safety Report feed (Jun 2026, iterations 39 — frontend 100%, backend curl+PDF verified)
- **Board Report FAIR-AIR (Feature 1)**: board-pack PDF (`POST /api/reports/board-pack.pdf`) now appends two sections — "AI Risk Quantification — FAIR-AIR" (5 vector scenarios with statement, why-it's-a-risk, key driver, NIST-mapped controls; evidence base cites International AI Safety Report 2026 (Bengio), IBM, DBIR, FAIR, NIST) and "NIST AI RMF (AI 100-1) — Controls Coverage & Compliance" (summary line + 16-row table). `build_board_report_pdf`/`_build_pdf` gained `fair_air`/`nist_coverage` params. Rendered & visually verified (pages 7-9).
- **Live Control Coverage (Feature 2)**: `GET /api/financial/nist-coverage` (`_nist_ai_rmf_coverage`) computes each of the 16 AI controls' coverage % from LIVE evidence — control effectiveness by category, latest self-scan score/KEV, scan-evidence alignment, connector/vendor/AI-system posture, calibration sign-off & autonomy engine state. Frontend `fair-nist-controls` now shows a green LIVE badge and uses these values (fallback = SAMPLE). Verified: overall 72%, 8/16 met, GOVERN 64/MAP 65/MEASURE 78/MANAGE 73.
- **Vector Deep-Dive (Feature 3)**: clicking any GenAI vector card opens a Dialog (`fair-vector-modal`) that runs `POST /api/advisor/fair-air/vector` (background) → `generate_fair_air_vector()` returns summary, expected loss, 2-3 scenarios, key drivers, and 3-5 NIST-mapped tailored mitigations. Verified (~12-15s).
- **Loss Exceedance Overlay (Feature 4)**: after AI analysis, the LEC chart overlays each AI scenario as a cyan `ReferenceDot` at its ($ loss, probability) with a caption. Verified (5 dots).
- **Claude → GPT + version selector**: all FAIR-AIR + board-report LLM calls switched from `anthropic/claude-opus-4-8` to `openai` GPT via emergentintegrations. Added a version selector (`fair-air-model-select`: GPT-5.6 Sol/Terra/Luna, 5.5, 5.4 default, 5.4-mini) with a compute-rationale note citing the International AI Safety Report 2026 (Fig 1.6 — more inference compute = more accurate). `_pick_model()` validates the allowlist; chosen model flows through both jobs and is returned in the badge.
- **International AI Safety Report 2026 feed**: `AI_SAFETY_EVIDENCE` (Bengio; why-AI-is-a-risk + risk categories) injected into `_all_dashboards_context`; system prompts + a new per-scenario `why_risk` field ground WHY each vector is a material risk alongside IBM/DBIR/FAIR; also cited in the PDF evidence base and the UI compute note.

## Dense live dashboards + AI-native details (Jun 2026, iterations 40-41 — frontend 100%, backend curl-verified)
- **Central dashboard service** `backend/dashboards.py` (`dash_router`, prefix `/api/dash`, wired in server.py). Endpoints (all LIVE, no seeded demo data): `/dash/assets` (per-host deduped + concurrent + 5-min cached enrichment: IPs, DNS, TLS, open ports, security headers score, technologies, CVEs/KEV/MITRE/CWE, exposure summary), `/dash/ai-analytics` (14-day usage trend/tokens/cost from advisor_logs, by-model, agent risk dist + guardrail coverage, OWASP-LLM Top 10), `/dash/vendors` (tier/category/data-access/region, attestation, incidents, renewals), `/dash/decisions` (rec funnel, by-area, coverage, decision register), `/dash/risk-register` (5x5 quantitative matrix, Monte-Carlo loss histogram + P10/ML/P50/P90/max markers, trending-vs-appetite band, top risks by $ exposure, control deficiencies WITH framework mapping, security initiatives, live threat-intel feeds KEV/ATT&CK/OSV/CWE, control effectiveness by domain, scan findings/vulnerabilities).
- **Rebuilt 5 dashboards** (dense, screen-filling, distinct domain accents — Asset=amber, AI Gov=rose, TPRM=teal, Decisions=violet, Risk Register=indigo): AssetIntelligence, AIGovernance (+ AIMonitor "AI monitoring & control techniques" card: inputs→AI system→outputs with human oversight / harmful-content detectors / sandboxing / CoT monitors driven by live guardrail coverage), VendorRisk, Decisions, RiskRegister (full portfolio reporting per reference images). Every card is always present with graceful empty states (never a blank page). Reusable primitives in `components/dash.jsx` (StatCard, CardShell, EmptyState, BarList, Spinner).
- **AI Analyst auto-summary** (`components/AIInsight.jsx`, `auto` prop): auto-runs `POST /api/advisor/insight` on mount on every dashboard, renders headline + cited findings (fact/estimate/prediction) + next actions; Analyze/Refresh button sits NEXT TO the title.
- **Per-entity AI risk rating + fix** (`POST /api/advisor/fix`, `components/AIFix.jsx`): rating Critical/High/Medium/Low + 0-100 score grounded in compliance-control gaps + CVE/KEV analysis (deterministic `_rating`), plus AI-written remediation + steps citing CVE ids and control frameworks (NIST/ISO/CIS). Embedded in the Asset detail modal and Risk detail pane. Cached 3min; deterministic fallback so the card always has an actionable fix.
- **Clickable details + network action buttons**: top highest-risk assets open `AssetDetailModal` (network metadata, vuln data, ports, headers, AI rating & fix). Risk detail pane is persistent (placeholder → populated). Admin action buttons wired to the network: `rr-autofix`/`risk-detail-autofix` → `POST /api/self-scan/autofix`; `rr-contain`/`risk-detail-contain` → `POST /api/self-scan/containment/scan`.
- **Obserrian Advisor redesign** (`components/AIAdvisor.jsx`): floating clutter (big pill + "Connect AI") replaced by a single pulsing corner eye-logo with a cycling "talking" bubble (proactive nudges incl. live top-risk callout from `/overview`); opens the advisor panel on click; auto-open-on-load removed.
- VERIFIED: all `/api/dash/*` + `/api/advisor/fix` curl-verified (asset→Medium/27, risk LR-001→Critical/74 with grounded rationale + NIST/ISO-cited remediation); iteration_41 frontend 100% (auto summaries on all 5 pages, advisor corner, action endpoints 200, asset modal, persistent detail pane); asset modal + risk pane AIFix render confirmed via screenshot.


## No-Mock truthfulness + performance + universal deep-dive (Jun 2026, iterations 45-46 — 100% tested)
- **Enterprise economics engine**: `risk_engine.correlate()` now emits `economics.{tprm,spend}` — TPRM Vendor Risk Premium (per-vendor modelled premium by tier × data-access × incidents; honest $0 empty state when 0 vendors) and Security-Spend Optimization (every modelled remediation $ tied to ALE reduction, blended ROI, best-ROI area). Exposed via `GET /api/risk-engine/economics` + inside `/strategic`. Rendered on `ExecBriefing.jsx` (Vendor Risk Premium + Security-Spend ROI tiles + `exec-tprm`/`exec-spend` sections).
- **No-Mock UI truthfulness**: `Remediations.jsx doAction()` no longer fakes success — it reflects the backend's real `verified`/`status`/`message` (green verified / amber in-progress / red not-applied). `RiskDetailModal.jsx` renders an honest `ActionResult` panel INSIDE the modal (status color, provider, message, ledger id, raw provider response toggle). For `dep-ecdsa` (CVE-2024-23342, no upstream fix) Execute Fix truthfully reports "no fixed release" and ALE stays $9.6M.
- **Defensibility Ledger UI** (`rem-ledger`) — lists every real remediation/verify attempt (`ledger-row-*`) with raw evidence toggle. **Automated Action-Verification Suite** (`rem-verify-suite`, `rem-verify-run`) → `POST /api/risk-engine/verify-connectors` (live Stripe HTTP 200; Clerk truthfully NOT CONFIGURED), results logged to ledger.
- **Performance / no-hang fix**: `ai_advisor._collect_stream()` wraps every LLM stream in `asyncio.wait_for` (insight 18s / explain 14s) with deterministic grounded fallbacks (`_insight_fallback`/`_explain_fallback`) so AI cards NEVER spin forever. `correlate(org_id, use_cache=True)` with a 6s TTL `_CORR_CACHE` collapses the 4 lens calls into one computation (Remediations page 1.2s; cached lens 0.1s). Inline cards use `CARD_MODEL=claude-sonnet-5`; axios 30s backstop timeout. Deep-dive modals render deterministic content INSTANTLY (score/rating/compliance chips/facets/recommended actions) while AI loads async.
- **Universal deep-dive + compliance alignment on every dashboard**: `RiskDetailModal` gained a `deep-dive-compliance` chips section (NIST/ISO/CIS + area coverage %). `AssetIntelligence.jsx` rewritten so every inventory row (`asset-<REF>`) opens the universal deep-dive with live security score, rating, compliance alignment, facets (IP/TLS/ports/CVE/KEV/exposure), grounded recommendations + AIExplain. Spend/Cyber/Risk/Vendor/AI-Gov/Compliance dashboards already carry detail/deep-dive modals.
- VERIFIED: iteration_45 (No-Mock/Ledger/Verify/Economics) + iteration_46 (perf no-hang + Asset deep-dive + regressions) both 100% — backend 9/9 pytest, frontend all acceptance criteria; deep-dive opens ~130ms, AIExplain resolves 5-12s, dep-ecdsa restored to Open.
- NOTE (open): login `OnboardingTour` overlay (z-9997) intercepts first-login clicks; global disclaimer still says remediation is "simulated" which now contradicts the live No-Mock pipeline — candidate copy update.

## Enterprise Connector Catalog + signed Ledger Export + performance (Jun 2026, iteration 47 — 100% tested)
- **Real connector catalog** (`connectors_catalog.py`, 36 providers / 10 categories: Clerk, Stripe, GitHub, Vercel, OpenAI, Anthropic, Synthesia, HeyGen, Slack, Teams, ServiceNow, SAP-SCIM, Okta, Cloudflare, Splunk, QRadar, CrowdStrike, Zscaler, Salesforce, PostgreSQL, etc.). Generic `_probe()` runs REAL authenticated connectivity checks (bearer/header/basic/webhook_post/query). Endpoints: `/api/connectors/catalog|discover|{id}/connect|{id}/test|{id}/disconnect`. Publishes a provider manifest (provider_id/version/capabilities) and normalized `health` (healthy/degraded/unavailable) + `failure_class` (unauthorized/invalid-configuration/rate-limited/…) + `connection_state` taxonomy. **NO-MOCK**: only marks connected on a real HTTP 2xx; otherwise truthful credentials_required/auth_failed/unreachable. Every probe logged to the Defensibility Ledger. In this env only Stripe auto-connects (STRIPE_SECRET_KEY); the other 35 honestly report "credentials_required" until the customer supplies keys.
- **Connectors dashboard rebuilt** (`AvailableConnectors.jsx`): "Auto-Discover & Connect" runs live probes across the catalog; grouped cards with honest status badges; per-connector Connect (credential form → real probe), Test, Disconnect. Replaced the old mock `enterprise.py` flag-flip connect. Existing live-cred connectors (M365/Copilot/OpenAI/Teams/SSO) preserved.
- **Signed Defensibility Ledger export** — `GET /api/risk-engine/ledger/export?format=csv|pdf|json`, each carrying a SHA-256 integrity signature over the exact rows (auditor-verifiable). PDF via reportlab. Export buttons (CSV/PDF) added to the Remediations ledger card.
- **Performance ("prime levels")**: gzip compression (`GZipMiddleware`, min 1024B) — connector catalog 67KB→8.9KB (7.5×), correlation payloads ~2.5×; `correlate()` cache TTL raised 6s→30s (4 engine-lens calls now ~0.09s each). Route-level code-splitting already in place (lazy per route). AI cards keep the hard timeout + deterministic fallback (no hang) and fast `claude-sonnet-5` model.
- VERIFIED: iteration_47 backend 9/9 pytest + frontend acceptance 100% (36 catalog cards, Stripe LIVE, 35 honest credentials_required, GitHub bogus token → AUTH FAILED, CSV/PDF/JSON export signed, No-Mock regressions intact). gzip + cache confirmed via curl; Overview renders end-to-end.
- NOTE: automated Playwright login-submit occasionally races the form re-mount (skips POST /api/auth/login); real users unaffected, API path solid. Not a functional bug.


## Live disclaimer + zero-touch device/user mapping + daily connector health cron (Jun 2026)
- **Footer disclaimer (Footer.jsx)**: removed "…remediation actions and connector syncs are simulated in this environment"; now reads "Automated remediation actions execute real, verified API calls and connector probes; every action is recorded to the Defensibility Ledger." Aligns copy with the live No-Mock pipeline. Frontend compiles clean.
- **Zero-touch asset discovery (`backend/asset_discovery.py`, NEW)**: `discover_and_map_assets(org_id)` probes every LIVE source and UPSERTS its inventory into `db.assets` tagged `discovered:True` — M365/Intune devices (name, owner, MAC via wiFi/ethernet, site from UPN domain / ownerType, OS, compliance; IP null when Graph doesn't expose it — honest), Entra ID users (`_m365_users`: role/jobTitle → privileged flag, userType→external, accountEnabled, signInActivity→last_active), and every catalog connector in `connector_state` (Connected → integration surface; auth_failed/unreachable/error → degraded). Re-runnable (upsert by ref). NO seeds — assets exist only from real live inventory.
- **Mapping into the engine**: `derive_asset_risks(assets)` turns discovered assets carrying genuine exposure (non-compliant devices→Endpoint Security, privileged/external/disabled identities→Identity & Access, degraded connectors→Third-Party Risk) into deterministic correlated risk records; `risk_engine.correlate()` appends them so they price into ALE, ratings, areas, exposure/tactical/compliance lenses. Portfolio now returns `discovered_ale` + `discovered_assets`. Extended `_m365_devices` `$select` to add userPrincipalName/wiFiMacAddress/ethernetMacAddress/managedDeviceOwnerType/azureADDeviceId.
- **Daily connector health cron (folded, no slot used)**: `_run_connector_health` rewritten to (1) re-verify legacy M365/Copilot/ChatGPT, (2) re-probe every catalog connector with saved creds via `connectors_catalog._probe`+`_persist`, (3) on silent credential expiry (was connected → not connected) post a **Slack/Teams alert** (`_post_chat_alert`, per-org `scan_alerts`) PLUS the existing email + in-app notice (deduped per day), (4) run `discover_and_map_assets`, (5) record the sweep to the Defensibility Ledger. Wired into the already-scheduled `daily-drift-digest` cron (08:00 UTC) — `.emergent/crons.yml` trimmed to exactly 5 entries (platform limit) with the daily description updated. The dedicated `/api/cron/connector-health` route is retained for manual/testing use.
- VERIFIED (Jun 2026): unit test injected 1 non-compliant device + 1 privileged user + 1 degraded connector → +3 derived risks, portfolio ALE moved +$4.65M, device appears in exposure lens (High, $2.4M); live curl confirms `/risk-engine/economics` + `/strategic` carry economics + discovered_ale/discovered_assets (no regression). Cron endpoint acks 200 with WEBHOOK_CRON_SECRET / 401 without; the live sweep mapped real Stripe (Connected) + GitHub (Degraded, from a prior bogus-token test) into db.assets and logged "all connectors healthy" to the ledger. Backend boots clean; frontend compiles clean.

## Platform-Wide Universal Interactive Cards — DONE (Jun 2026)
User's FINAL COMMAND satisfied: every KPI/summary card across all dashboards is now clickable and opens the standardized deep-dive (live rating/score, grounded AI recommendations, honest No-Mock action hub).
- **NEW `src/context/DeepDiveContext.jsx`**: single `<RiskDetailModal>` mounted app-wide via `DeepDiveProvider` (mounted in `AppShell.jsx`). Exposes `openDeepDive(item)`; manages busy/result; passes `onAction` only when an item carries a real async `onAction` (so pure KPI cards honestly show "No open remediation task is linked" instead of a fake Execute button — per user's No-Mock directive).
- **`src/components/dash.jsx`**: `StatCard` is now a clickable `<button>` (auto-builds a detail from the metric when none is passed → instantly makes all shared-primitive dashboards interactive: SituationRoom, Decisions, RiskRegister, Remediations, AssetIntelligence, VendorRisk, AIGovernance). `CardShell` gained optional `detail` (clickable header). NEW `ClickCard` wrapper makes bespoke legacy cards clickable without changing their markup.
- **Bespoke pages wired**: Overview (MetricCard), ControlMonitoring (KPI + framework + analytics ClickCards), CompliancePosture (Overall alignment + Alignment-by-framework panels), Enterprise (readiness estat cards), SituationRoom (AI incident rows), KernelStatus (remediation KPIs, subsystem, policy, workflow cards — policy edit uses stopPropagation), CyberRisk (inner FAIR `Kpi` cards; main Stat cards keep their existing cyber-metric-modal).
- Page-owned rich modals preserved (Asset Intelligence rows, Cyber risk rows, Situation exposures, Compliance ControlDetailModal) — only previously-static cards got the new behavior.
- Fixed duplicate-React-key warning globally in `RiskDetailModal` facet render (`key={label-index}`).
- VERIFIED via testing_agent iteration_49.json: 100% of card + regression assertions passed across 11 dashboards; exactly one deep-dive-modal in DOM at a time; AI insight resolves < ~15s (grounded, non-spinning); all existing row/detail modals still work. Remaining: only a cosmetic dev-instrumentation `<span>`-in-`<option>` warning on two pre-existing selects (not introduced here).

## Follow-ups: Tour, Studio Report Types, AI Preload, Spend Deep-Dives, $-at-Stake — DONE (Jun 2026)
- **Onboarding tour non-blocking** (`OnboardingTour.jsx`): outer overlay is now `pointer-events-none` so new admins can click KPI cards immediately without dismissing the tour; the tour dialog itself stays interactive.
- **Studio full-story report types** (`studio.py` + `Studio.jsx`): NEW `GET /studio/report/types` (story / board / cfo / soc). Each preset one-click sets title + sections and auto-composes; `_ai_narrative(...report_type)` varies persona + length (story ≈230 words end-to-end enterprise narrative). `compose` accepts `report_type`; response carries it; output header tags the type.
- **Card AI preload / warm-on-hover** (`AIExplain.jsx` module cache + `prefetchExplain`, `DeepDiveContext.warm`, cards call `warm()` onMouseEnter): hovering a card pre-fetches its grounded brief so the deep-dive shows insight + $ impact instantly on click.
- **Spend deep-dives** (`SpendGovernance.jsx`): `sg-budget-deepdive` (Monthly budget) and `sg-teammate-deepdive` (Spend by teammate) open the universal deep-dive with grounded cost-governance recommendations; existing month-card / per-user modals unchanged.
- **$ at stake / reduction if fixed on every deep-dive** (`ai_advisor.py` `_deep_num`/`_impact_estimate`, returned on BOTH LLM-success and fallback paths; rendered by `AIExplain` `ai-impact` banner): item-scope real $ where the card carries it; else honest portfolio residual-ALE (`$10.22M` live) with a modelled ~35% reduction — labelled `item`/`portfolio`/`modelled`, never fabricated. Fixed a bug where the LLM-success path was missing `data.update(impact)`.
- VERIFIED via testing_agent iteration_50.json: all 5 items 100% pass; regressions intact (spend month-card, report-compose, section toggles, PDF export). Backend curl confirms item/portfolio/modelled scopes + cached responses retain fields.

## Spend Alerts + Add-to-Remediation-Plan — DONE (Jun 2026)
- **Slack/Teams spend ping** (`ai_advisor.py` `_check_budget` forecast branch): when projected month-end advisor spend is on track to exceed the monthly cap, in addition to the existing in-app + email alert it now posts a `_post_chat_alert` to the org's Teams/Slack webhook (deduped once per month via `advisor_forecast_notified`). Code-verified (`_post_chat_alert` exists + dedupe); live trigger requires a configured webhook + forecast-over.
- **"Add to remediation plan" from any deep-dive** (`RiskDetailModal.jsx` AddToPlanButton → `POST /risk-engine/plan`): one click turns any card into a tracked task. NEW `db.remediation_plan` collection + endpoints `POST /risk-engine/plan`, `GET /risk-engine/plan`, `POST /risk-engine/plan/{id}/status`. The stored $ at stake / reduction is grounded via the same `_impact_estimate` (no fabrication). Surfaced on the Remediations page in a new "Remediation Plan — added from deep-dives" card (`rem-plan`) with per-task status dropdowns (Open/In Progress/Remediated/Accepted) and the $ pill.
- VERIFIED: curl create→list→status all 200 (portfolio $10.22M, modelled $3.58M reduction); screenshot confirms button → "Added to plan" toast and the task appears on Remediations with status control. Test artifacts cleaned from the live org.

## SAP UAC — Ticket cleanup, bulk remediation, workflow feed, SoD auto-remediation, JML/Activation details + backend modularization (Jun 2026, iteration_56 — backend 7/7, frontend 5/5, 100%)
Delivered the user's 4 backlog items + the approved SoD auto-remediation engine + JML/User-Activation "Obserra Standard" gaps:
- **Uniform ticket shape**: `_ticket_public()` now returns `{number,type,state,systems_touched}` on EVERY action endpoint (accounts/action, accounts/bulk-action, privileged, roles, requests decide/provision, reconcile, activation/set tickets[]). Legacy `systems` key fully removed.
- **Bulk from Access Monitoring**: per-row checkboxes + select-all + bulk bar (Lock / De-provision / Recertify) → `POST /api/sap/accounts/bulk-action` (one live ServiceNow workflow per account). Shared `_run_account_action()` helper backs single, bulk and auto-remediation.
- **Workflow Activity feed** (new page `/app/workflow`, nav "Workflow Activity"): live filterable stream of every ServiceNow ticket (search / prefix REQ·INC·CHG / system / timeframe), summary StatCards, row→stage-timeline modal. `GET /api/sap/workflow/activity`.
- **SoD → ServiceNow Auto-Remediation engine** (card on SoD Command Center): toggle + trigger-severity chips + action select + candidate gauge + "Run now" + recent-remediation log. `GET/PUT /api/sap/autoremediation`, `POST /api/sap/autoremediation/run`. Deduped/idempotent per account; live auto-hook fires on `GET /sod/conflicts` when enabled (closes risk with no human click).
- **JML (Lifecycle)**: NEW Movers panel (in-flight transfers derived from real ADP↔IZ8 org-attribute disagreements via `_hr_conflicts_for`), clickable deep-dives on joiners/movers/leavers, and per-row Deactivate/Suspend/Reactivate workflow buttons on every row. Backend `jml()` now populates `movers`.
- **User Activation**: each user name is now clickable → per-user detail modal (`/api/sap/identities/{user_id}`) showing risk, activation status, accounts+roles, SoD conflicts and a status-aware ServiceNow action bar.
- **Backend modularization**: split the 2,473-line `sap_uac.py` into `sap_data.py` (127 — canonical domain model & constants) + `sap_engine.py` (417 — live No-Mock compute engine: seed/correlate/risk/HR/flags) + `sap_uac.py` (1,958 — routes, ServiceNow builders, activation, auto-remediation, AI). One-way dependency (data ← engine ← routes); `server.py` import unchanged (re-exported via `import *`). All 15 endpoints verified 200 with identical data; every live action path re-verified through the split modules.
- Known: one cosmetic Radix `DialogTitle` a11y console warning (non-functional). `RiskDetailModal` is a custom overlay (not Radix), so not the source.

## SAP UAC — Universal action parity: Identities + Access Monitoring live workflows (Jun 2026, iteration_55 — backend 6/6, frontend 5/5 flows, 100%)
- **Obserra Standard everywhere**: every SAP UAC dashboard = AI Insight card + clickable details + LIVE ServiceNow cross-system action buttons (ServiceNow → ADP/IZ8 HR → SAP → AD/Entra).
- **Identities.jsx (rewritten)** — detail modal now carries full action parity:
  - Person-level lifecycle **action bar** (`id-action-bar`) rendered by activation status via `PERSON_ACTIONS` (Activated⇒Suspend+Deactivate, Suspended⇒Resume+Deactivate, Deactivated⇒Activate) → `POST /api/sap/activation/set` (returns `tickets[]`). Confirm dialog (`id-confirm-dialog`) with reason (`id-reason`) + email-notify (`id-notify`, person scope only); status badge (`id-activation-status`) flips + modal refreshes after action.
  - Per-SAP-account **Lock** (`id-acc-lock-<ref>`, hidden when already locked) + **Recertify** (`id-acc-recertify-<ref>`) → `POST /api/sap/accounts/{ref}/action`; account-scope confirm hides the notify checkbox.
  - Toasts cite the real ServiceNow ticket number that was opened & auto-closed.
- **Backend**: `GET /api/sap/identities/{ref}` now returns `activation_status` (from `sap_activation` override via `_activation_status`).
- **Access Monitoring** (`AccessMonitoring.jsx` + `POST /api/sap/accounts/{ref}/action`) verified end-to-end — dormant/orphan/service tabs, per-row Lock/De-provision/Recertify fire live workflows.
- **Connectors all live**: `GET /api/sap/systems` returns 43 connectors, ALL `status=connected` + `auth_ready=true` (7 SAP UAC + 36 Obserra Platform catalog).
- Note (non-blocking): `account/action` returns `ticket.systems` while `activation/set` returns `tickets[]` — naming could be standardised. `sap_uac.py` is ~2270 lines — refactor into modules (identities/activation/accounts/monitoring/systems/tickets/insight) recommended.

## v1 release packaging + in-app PWA install (Jun 2026)
- Repo made GitHub-ready: rewrote `README.md`, added `backend/.env.example` + `frontend/.env.example` (no secrets, un-ignored in `.gitignore`). Clean source zip built at `release/obserra-v1-source.zip` (excludes real `.env` + `node_modules`). Existing one-command on-prem Docker installer confirmed at `deploy/onprem/`.
- **In-app "Install app" button** (`components/InstallButton.jsx`, mounted in `AppShell` header): captures `beforeinstallprompt` and fires the native PWA install on click (Chrome/Edge/Android); iOS Safari shows an Add-to-Home-Screen hint. Renders nothing once installed/standalone. VERIFIED via synthetic `beforeinstallprompt` — button appears and invokes the prompt. (Distribution guidance: deploy → live URL/PWA is the cross-device "install"; a zero-dependency offline `.exe`/USB installer isn't possible for a live web app.)

## SAP UAC — Workflow Export + Mover Role Diff + Governance Digest + Scheduled Auto-Remediation (Jun 2026, iteration_57 — backend 12/12, frontend 4/4, 100%)
Delivered the 4 approved backlog features:
- **Workflow Export (CSV / PDF)** — `GET /api/sap/workflow/activity/export?format=csv|pdf` (honours the same q/prefix/system/days filters). CSV = full ticket ledger; PDF = branded landscape "SAP Access Governance — Workflow Evidence Pack" (reportlab: eye badge, navy header, wrapping table, alternating rows, confidential footer) for SOX evidence. UI: `wf-export-csv` / `wf-export-pdf` buttons on Workflow Activity (blob download + toast). PDF visually verified (document-verification skill).
- **Mover Role Diff + 1-click strip** — `jml()` already returns `current_roles` / `birthright_roles` / `carried_over`. NEW `POST /api/sap/jml/{ref}/strip-carried-over` pulls every non-birthright (carried-over) role from the mover's accounts and opens a ServiceNow → HR → SAP "SAP Mover Access Cleanup" CHG (auto-closed); 400 when nothing to strip, 404 unknown. UI: Movers rows show a "N carried over" badge; clicking opens `jml-mover-dialog` — side-by-side "Roles held now" (CARRIED OVER red vs BIRTHRIGHT green) vs "<dept> birthright" (HELD vs NOT PROVISIONED); `jml-mover-strip-btn` (disabled when 0) + `mover-ai-review` (AI deep-dive).
- **SAP Governance Digest email** — `POST /api/sap/governance-digest/send` (on-demand, `autorem-digest` button on SoD Command Center) emails admins/execs a branded daily posture digest (open SoD by severity, auto-remediated 24h, ServiceNow workflows 24h, residual-access leavers, top risks, avg score) via managed Resend (`notifications.send_email`). Per-org **60s backoff** (`sap_digest_state`) returns `{throttled:true, sent:0}` to avoid colliding with the folded cron and tripping Resend's rate limit.
- **Scheduled unattended auto-remediation + digest (no cron slot used)** — `.emergent/crons.yml` is at the platform 5-cron hard limit, so `run_sap_autoremediation_all()` (sweeps every org with the SoD engine enabled, records `last_cron_at`/`last_cron_count`) + `run_sap_governance_digest()` were **folded into the existing `daily-drift-digest` cron** (08:00 UTC) as background tasks (same pattern as connector-health). Verified: cron acks 200 with `Bearer WEBHOOK_CRON_SECRET`, 401 without. SoD card now shows the last scheduled-sweep line.
- **Seed enrichment**: `sap_engine.seed_sap_uac` appends 2 deterministic mover-accumulation scenarios (transfer conflict + a role outside the current dept's birthright) so the mover diff/strip is demonstrable for every org; `_build_person` gained `force_conflict`. Existing population regenerates identically (RNG seeded by org_id).
- Known (non-blocking): cosmetic Radix `DialogTitle` a11y warning persists; `sap_uac.py` is ~2260 lines — module split (workflow/jml/digest/autoremediation) recommended.

## SAP UAC — Connector Health Depth + Digest Scheduling + Slack/Teams alert + Mover Auto-Strip Rule (Jun 2026, iteration_58 — backend 15/15, frontend 3/3, 100%)
Delivered the approved 4-feature functional batch (Task 1). All connections are LIVE (no mocks) — Slack/Teams posts are real HTTP calls, ServiceNow ticket + Resend email flows are the existing live pipelines.
- **Connector Health Depth** (`SapSystems.jsx` "Connector Health", `/app/systems`): surfaced the existing backend depth — "Re-probe all connectors" button (`sys-reprobe` → `POST /api/sap/systems/reprobe`), health summary tiles (`sys-health-healthy/stale/degraded` from `connector_health`), per-connector health badges (`sys-conn-health-<id>`), sync-drift notes (`sys-conn-drift-<id>`, age/stale/degraded reasons) and `sys-last-probe` timestamp. Re-probe refreshes all 43 connectors to healthy.
- **Digest Scheduling Controls** (`SodCommandCenter.jsx` card `sod-digest-schedule`): `GET/PUT /api/sap/digest/config` (PUT admin-only) — enable/disable the daily digest (`digest-enable`), send-window days everyday/weekdays (`digest-days`, honored by the folded daily 08:00 UTC cron), recipients override (`digest-recipients`, blank = admins/execs), chat-alert toggle (`digest-chat-toggle`), and **dedicated SAP Teams/Slack webhook fields** (`digest-teams-url`/`digest-slack-url`) that take priority over the org `scan_alerts`/`live_teams` fallback. `Save`, `Test chat alert` (`POST /api/sap/digest/test-chat`), and `Send digest now` (existing 60s-backoff send). `run_sap_governance_digest` now honors enabled/days/recipients and posts the chat summary when enabled.
- **Slack/Teams digest alert**: `_sap_post_chat(org_id, cfg, title, text)` resolves dedicated SAP webhook → org scan_alerts.teams_url → live_teams → slack_url and posts a real MessageCard/Slack payload; wired into both the scheduled digest and the on-demand send. `_digest_chat_text` builds the one-line posture summary. Verified live: `test-chat` returned `posted:true` against the demo org's configured webhook.
- **Mover Auto-Strip Rule** (`Lifecycle.jsx` Movers panel): `GET /api/sap/mover-rule` + `PUT /api/sap/mover-rule` (admin) + `POST /api/sap/mover-rule/run`. Toggle (`mover-rule-toggle` + `mover-rule-state` chip) enables zero-click least-privilege — carried-over roles are auto-stripped for every in-flight mover via a shared `_strip_carried_over(org_id, p, by, reason)` helper (refactored out of the manual strip endpoint). Enabling runs immediately and also auto-hooks on every `GET /api/sap/jml` (idempotent). `run_sap_mover_autostrip_all()` folded into the existing `daily-drift-digest` cron (no new slot; still at 5-cron limit).
- **Seed/demo note**: the mover-accumulation scenario (P-0045 Procurement holds Sales role `Z_SD_BILLING`; P-0046 Finance holds Procurement role `Z_MM_PO_APPROVER`) is re-injected for the demo org after testing so the Mover Role Diff + strip button + auto-strip toggle stay demonstrable (candidates=2, rule OFF by default).
- iter58 fix (by testing agent): `SodCommandCenter.jsx` was missing `CalendarClock`/`Send` (lucide) + `Input` imports — added; page + all 9 digest testids render, all 3 digest API actions return 200. Backend pytest `tests/test_iter58_governance.py` 15/15.

## SAP UAC — Digest Preview + Mover Rule Report + Throttle Hint + Governance Scorecard (Jun 2026, iteration_59 — backend 12/12, frontend 4/4)
- Digest Preview: GET /api/sap/digest/preview + in-app dialog (SoD Command Center).
- Mover Rule Report: auto-strip activity-log card on Lifecycle (uses GET /api/sap/mover-rule log).
- Throttle Hint: countdown on 'Send digest now' from digest last_at + 60s backoff.
- Access Governance Scorecard: GET /api/sap/scorecard (weekly trend, real≥2 snapshots else derived) + GET /api/sap/scorecard/export?format=csv + recharts card + record_sap_scorecard_all folded into daily cron. Governance score rebalanced (weighted, ~53 for demo org — not flatlined).

## SAP UAC — Backend modularization of sap_uac.py (DONE, Jun 2026)
- Split the 2,658-line `sap_uac.py` monolith into domain modules (shared core stays in `sap_uac.py`: router, `_correlate`, `_snow_generic`, `_run_account_action`, `_audit`, `_ensure`, `_ticket_public`, data). `sap_uac.py` is now 1,860 lines.
  - `sap_autoremediation.py` (122 lines): SoD→ServiceNow auto-remediation engine + `run_sap_autoremediation_all`.
  - `sap_jml.py` (190): Joiner/Mover/Leaver + Mover Auto-Strip rule + `run_sap_mover_autostrip_all`.
  - `sap_workflow.py` (165): ServiceNow workflow activity stream + CSV/PDF evidence export.
  - `sap_digest.py` (383): Governance digest (email + Slack/Teams) + Access Governance Scorecard + `run_sap_governance_digest` + `record_sap_scorecard_all`.
- Pattern: domain modules import the shared `sap_router` + core helpers from `sap_uac`; `sap_uac` imports the domain modules at the END (routes register + background jobs re-exported so `scheduled.py` still imports them from `sap_uac` unchanged). Kept `sod/conflicts` uses a function-local import of the auto-remediation helpers to avoid a circular import.
- Verified: 56 SAP routes register, all endpoints return 200 live, both pytest suites (27 tests) pass, `server`/`scheduled` import cleanly.

## SAP UAC — Reporting batch: Scorecard PDF + real trend + digest attach + weekly scorecard + Mover Rule Report filters (Jun 2026, iteration_60→61)
- **Scorecard PDF** (`GET /api/sap/scorecard/export?format=pdf`) — branded one-page governance scorecard (reportlab); CSV export also available. SoD Command Center card exposes both download buttons (LIVE TREND chip).
- **History backfill** — seeded 8 weeks of real scorecard snapshots so `/api/sap/scorecard` returns `trend_source='real'` with ascending points instantly.
- **Digest attachment** — the daily governance digest email now attaches the latest scorecard CSV (`notifications.send_email`).
- **Weekly Scorecard email** — Monday leadership email with inline trend, folded into `sap_digest` (`run_sap_weekly_scorecard`, re-exported from `sap_uac`); still at the 5-cron platform limit so scheduled via existing background system.
- **Mover Rule Report filters (iter61 fix)** — `GET /api/sap/mover-rule?q=&days=` filters the auto-strip activity log by search text (name/dept/ticket/role) and time window. `Lifecycle.jsx` now renders the two controls in the Auto-Strip Activity Log header (shown when `stripped_total>0`): `mover-log-search` Input + `mover-log-days` Select (All time / 7 / 30 / 90). State (`logQ`/`logDays`) + `loadRule()` query-param wiring already existed; iter60 testing caught the JSX was never rendered — now added and verified E2E (typing "Vikram" narrows 2 rows → 1; backend q/days confirmed via curl).
- Backend all green (iter60 16/16 pytest). Demo state after this fix: mover-rule OFF, log empty, P-0045 `Z_SD_BILLING` + P-0046 `Z_MM_PO_APPROVER` re-injected (strip demo replayable; filters appear after first auto-strip).

## SAP UAC — Scorecard Alerts + Scheduled SoD Evidence Export + Rule Report Export + Trend Annotations (Jun 2026, iteration_61)
Delivered the 4 user-requested governance-reporting features (all live, no mocks):
- **Scorecard Alerts** — `POST /api/sap/scorecard/alert-check` (admin) + daily `run_sap_scorecard_alerts()` (folded into daily cron). Posts a Slack/Teams alert (via `_sap_post_chat`) + in-app notification the moment the governance score drops below the org's target threshold. De-duped per ISO week (`sap_digest_state.score_alert_week`); recovery clears the flag so the next drop re-alerts. Config: `score_alert` + `score_threshold` (default 60) on the digest config. UI: `score-alert-config` section in the SoD Command Center digest card (threshold input + toggle + "Check & alert now").
- **Scheduled SoD Evidence Export** — `_sod_evidence_rows/_csv/_pdf`, `GET /api/sap/sod-evidence/export?format=csv|pdf&status=&severity=`, `POST /api/sap/sod-evidence/send` (admin, email now), and weekly `run_sap_sod_evidence_export()` (folded into daily cron; runs on each org's configured `evidence_day`). Branded SOX-grade PDF lists every SoD conflict with its toxic function combination, granting roles, and remediation state. Config: `evidence_export` + `evidence_recipients` + `evidence_day`. UI: `evidence-export-config` section (toggle + day select + recipients + Send now / Download PDF / Download CSV). PDF visually verified (document-verification skill — 72 conflicts, branded table).
- **Rule Report Export** — `GET /api/sap/mover-rule/export?format=csv|pdf&q=&days=` (`_filter_mover_log`, `_mover_log_pdf`) exports the FILTERED mover auto-strip log as CSV or branded PDF. UI: `mover-log-export-csv` / `mover-log-export-pdf` buttons beside the filters in Lifecycle's `mover-rule-report`. Filter (q=Vikram) honored in export — visually verified (1 record).
- **Trend Annotations** — `_annotate_trend()` adds a `note` + `changes[]` (Gov delta, SoD opened/resolved, +auto-remediated, +movers cleaned, residual added/cleared) + `movers` to every scorecard trend point. UI: custom `TrendTip` chart tooltip shows the note; `scorecard-annotations` "What changed week-over-week" block lists per-week change chips.
- Both new background jobs re-exported from `sap_uac` and added to the daily-drift-digest cron (still within the 5-cron platform limit — folded, no new slot).
- iter61 fix: `DigestConfigBody` Pydantic model had silently NOT persisted the 5 new fields on first edit → PUT `/digest/config` 500. Re-applied; PUT now 200 and config persists (verified via curl). Frontend `digest-save` error toast no longer misreports 500 as "admin only".
- Testing: frontend testing agent verified all 22 new data-testids present/interactive, downloads + filtered export + annotations + tooltip working, chat webhooks not regressed (iteration_61.json). Backend curl-verified 100%.

## SAP UAC — Alert History + Custom Thresholds + Evidence Signoff + Evidence Preview + "Why did the score move?" AI + Audit-Log AI box (Jun 2026, iteration_62)
Delivered 5 user-requested governance features + extended the AI box to Audit Log (all live, no mocks; frontend agent 100% pass, iteration_62.json):
- **Custom per-severity thresholds** — `sev_thresholds` {Critical:25, High:50} on the digest config alongside `score_threshold`. `_check_score_alert` now returns a `reasons[]` list and fires when the governance score is below target OR any severity's open-conflict count exceeds its limit. UI: `sev-threshold-Critical`/`sev-threshold-High` inputs in the score-alert card.
- **Alert History** — every fired alert is written to `sap_score_alert_log` (score, date, reasons, posted). `GET /api/sap/scorecard/alerts`. UI: `score-alert-history` "Recent alerts" list; `score-alert-check` toast now lists the exact reasons that tripped.
- **Evidence Signoff** — `_sod_evidence_pdf(rows, summary, signed_by)` stamps a green "REVIEWED & SIGNED — <name> · <date>" box (or a blank wet-sign line when unsigned) on the last PDF page. `evidence_signed_by` config field + `evidence-signed-by` input; flows into `GET /sod-evidence/export?signed_by=`, `POST /sod-evidence/send` (SodEvidenceBody), and the weekly cron. PDF stamp visually verified.
- **Evidence Preview** — `GET /api/sap/sod-evidence/preview` returns the email HTML + summary + first 25 conflicts + recipients + send-day + signed_by. UI: `evidence-preview` button → `evidence-preview-dialog` with email body, meta line, and `evidence-preview-table`.
- **"Why did the score move?" AI summary** — `GET /api/sap/scorecard/why` returns a one-sentence LLM (openai/gpt-5.4) explanation grounded in the 8-week deltas, with `_score_why_fallback` deterministic fallback. UI: `scorecard-why` box above the trend + `scorecard-why-refresh`.
- **Audit Log AI box** — added `<SapInsight dashboard="Audit Log" slug="audit-log" />` so the Obserra-standard AI analyst box now appears on the Audit Log page too (chosen scope: the one remaining SAP-data page; Team/Settings/Marketplace/Billing intentionally left without it as non-dashboard admin pages).
- iter62 fixes: two silent `search_replace` non-persists caught & re-applied (the `/scorecard/alerts` route and the `scorecard-why` JSX box); one orphaned-garbage cleanup after an append. All verified via curl + PDF render + frontend agent.

## SAP UAC — Alert Muting + 2-Step Evidence Signoff + Score Forecast + Per-Auditor Packs (Jun 2026, iteration_63)
Delivered the 4 user-requested SoD Command Center features (all live, no mocks; testing_agent 100% — backend 5/5 pytest + frontend E2E, iteration_63.json):
- **Alert Muting** — `POST /api/sap/scorecard/alert-mute {hours,reason}` + `/alert-unmute` (admin). Muted state on `sap_digest_state` (`alert_mute_until`/`alert_mute_reason`); `_check_score_alert` respects it (returns `muted:true`, no post while snoozed). `GET /scorecard/alerts` returns `muted`/`mute_until`/`mute_reason`. UI: `score-mute-banner` (shows until-date + reason + `score-unmute`), `score-mute-24h`/`score-mute-7d` snooze buttons in `score-alert-config`.
- **Two-step Evidence Signoff** — `evidence_prepared_by` (config) + `POST /sod-evidence/approve {approved_by}` / `/unapprove` (admin). Changing "Prepared by" clears any prior approval. `_sod_evidence_pdf(..., prepared_by, approved_by, approved_at)` stamps both names + approval date. UI: `evidence-signoff` block (`evidence-prepared-by`, `evidence-approve`/`evidence-unapprove`, `evidence-approval-status` chip).
- **Score Forecast** — `_forecast(trend, current)` projects next-week governance score from the recent weekly pace; returned as `scorecard.forecast {next_week_score, delta, basis}`. UI: `scorecard-forecast` chip in the "Why did the score move?" box (green/red by delta sign).
- **Per-Auditor Packs** — `auditor_scopes[]` ({email, areas[], systems[]}) on the digest config; `_scope_rows()` filters evidence rows per recipient. `POST /sod-evidence/send` and the weekly cron deliver each auditor a pack scoped to their areas/systems (blank = full pack). `GET /sod-evidence/preview` returns `recipients_detail[]` with per-recipient conflict counts + scope tags. UI: `auditor-scopes` editor (`auditor-scope-add`/`-email-i`/`-areas-i`/`-systems-i`/`-remove-i`) + `evidence-preview-recipients` in the preview dialog.

## SAP UAC — "Ask AI about this digest" in-app chat panel (Jun 2026, iteration_63b)
Replaces the deferred reply-to-email idea (no inbound-email infra needed). Verified via curl (intro + ask + multi-turn session) + screenshot (full open → suggestion → grounded answer flow):
- **Backend** (`sap_digest.py`): `GET /api/sap/digest/ask/intro` (greeting + suggested questions) and `POST /api/sap/digest/ask {session_id, question}`. `_digest_ai_context()` assembles a grounded live snapshot (open-SoD by area/system, top rules, scorecard current/forecast/trend, residual, auto-rem). LLM = openai/gpt-5.4 via emergentintegrations (same pattern as `/scorecard/why`), with `_digest_ask_fallback` deterministic fallback. Multi-turn: conversation stored in `sap_digest_chat` keyed by (org_id, session_id), last 6 turns re-injected as context. Audited as `sap.digest.ask`.
- **Frontend** (`SodCommandCenter.jsx`): `digest-ask-open` button in the Governance Digest Schedule actions row opens `digest-ask-dialog` — a chat UI with greeting, `digest-ask-suggestions` chips, `digest-ask-messages` bubbles, `digest-ask-input`/`digest-ask-send`. Answers are grounded (verified: cites Basis/Security 22, Finance 18, S4P 39, top-risk identities, toxic rule patterns).

## SAP UAC — Chat Export + Shareable Read-only Link + Voice Digest (Jun 2026, iteration_64)
Three user-approved add-ons to the digest experience (backend 100% curl-verified; frontend testing_agent + self-test verified; Slack Ask was explicitly skipped):
- **Chat Export (PDF + Email)** — the AI Q&A thread (`sap_digest_chat`) can be saved. `GET /api/sap/digest/ask/export?session_id=` returns a branded `_chat_pdf` (Q/A styled, ~65KB). `POST /api/sap/digest/ask/email {session_id, recipients[]}` emails an HTML thread (`_ask_email_html`) with the PDF attached via Resend. Both write `sap.digest.ask.export` / `sap.digest.ask.email` to the audit trail. UI: `digest-ask-actions` row inside the ask dialog (`digest-ask-export` Download PDF, `digest-ask-email` Email thread — recipients via prompt, prefilled from digest recipients), shown once the thread has ≥1 question.
- **Shareable read-only Link** — `POST /api/sap/digest/share` builds a static snapshot (`_build_digest_snapshot`: digest KPIs + scorecard current/forecast/trend + deterministic AI `why` summary) + tokenised record in `sap_digest_shares` (14-day expiry), returning a `FRONTEND_URL`-based `/share/digest/{token}` URL. Public `GET /api/sap/public/digest-share/{token}` (NO auth). New public page `ShareDigest.jsx` (route in `App.js` OUTSIDE the Gate) renders a branded, login-free read-only snapshot — no live asking (per user choice). The governance digest email now embeds a "View the live governance snapshot →" CTA (`_governance_digest_html(d, share_url)`; both `governance_digest_send` and `run_sap_governance_digest` create a share and pass its URL). UI: `digest-share` "Copy share link" button (copies to clipboard + toast).
- **Voice Digest** — `GET /api/sap/digest/voice` returns an `audio/mpeg` spoken briefing generated by OpenAI TTS (`OpenAITextToSpeech`, model `tts-1-hd`, voice `onyx`, via `EMERGENT_LLM_KEY`), cached in `sap_digest_voice` by SHA-256 script hash to avoid regen cost. `_digest_voice_script` builds a grounded ~500-char narration; `GET /digest/voice/script` exposes the transcript. UI: `digest-voice` "Listen to digest" button → inline `digest-voice-player` (`<audio>` + `digest-voice-download` .mp3). Fallback: 503 with a friendly retry message if TTS is unavailable.
- iter64 fix: the `digest-share` + `digest-voice` button JSX (and, earlier, their state decls) silently failed to persist during the multi-edit — re-applied via `insert_text` and re-verified (voice player renders with audio + Download .mp3; share toasts the /share/digest URL). Recurring silent-`search_replace`-non-persist pattern on this large file — always grep-confirm the diff landed after editing `SodCommandCenter.jsx`.

## SAP UAC — Voice Controls + Voice-on-Send + Chat History + Share Analytics (Jun 2026, iteration_65)
Four user-approved add-ons (backend curl-verified; frontend testing_agent iteration_65 = 100% after one fix). All defaults per user choice.
- **Voice controls** — digest config gains `voice_name` (onyx default; alloy/nova/shimmer/echo) + `voice_speed` (1/1.25/1.5), validated in `put_digest_config` against `_TTS_VOICES`. `GET /api/sap/digest/voice?voice=&speed=` honors params or saved config via `_generate_voice_audio` (cached per `script|voice|speed`). UI: `voice-config` block with `voice-name`/`voice-speed` Selects; `playVoice` passes the current selection.
- **Voice-on-Send** — `voice_attach` bool (off by default) toggled via `voice-attach-toggle`. When on, `governance_digest_send` + the daily cron `run_sap_governance_digest` attach `sap-governance-briefing.mp3` (from `_generate_voice_audio`) to the digest email.
- **Chat History** — `GET /api/sap/digest/ask/history` (recent threads: session_id, title=first Q, count, updated_at) + `GET /api/sap/digest/ask/thread?session_id=` (full messages). UI: `digest-ask-history-toggle` in the Ask-AI dialog opens `digest-ask-history` list; `openThread` loads a past thread into the dialog and follow-ups continue it (same session_id).
- **Share Analytics** — `public_digest_share` now `$inc opens` + records `last_opened_at`/`opened_events`. `GET /api/sap/digest/shares` returns the org's links with opens/last-opened. UI: `digest-shares` block (summary + per-row `digest-share-opens-i` + Copy); refreshed after `createShare`.
- iter65 fix (QA agent): `History` lucide icon was used but not imported → 'Illegal constructor' crash of the Ask-AI dialog; added `History` to the lucide-react import.

## P2 Component Cleanup — DECLINED by user (Jun 2026)
User chose to **skip** splitting `SodCommandCenter.jsx` (~1000 lines) and `sap_digest.py` (~1720 lines) into sub-modules. Rationale: internal-only maintainability with real regression risk — the digest card is tightly coupled to `scorecard`/`dcfg` + ~15 shared handlers, the backend render builders are scattered (not contiguous), and the edit tool silently failed to persist on these two files 4× this session. App is stable and 100% green; revisit only if these files actively block future work.

## SAP UAC — Voice Preview + Thread Titles + Open Alerts + Share Briefing + Weekly Recap (Jun 2026, iteration_66)
Five user-approved add-ons (backend curl-verified; frontend testing_agent iteration_66 = 100%, zero issues). All defaults per user choice.
- **Voice Preview** — `GET /api/sap/digest/voice/sample?voice=` returns a short spoken sample (fixed phrase, `tts-1`, cached globally under org_id `_sample`). Frontend auto-plays it (via `new Audio()` + `previewAudioRef`) the moment a leader changes the `voice-name` Select.
- **Thread Titles** — `POST /api/sap/digest/ask/rename {session_id, title}` stores `title` on `sap_digest_chat`; `digest_ask_history` now returns the stored title (+`custom` flag). UI: per-row rename button (`digest-ask-rename-i`) in the Ask-AI History list (window.prompt).
- **Open Alerts** — `public_digest_share` detects the FIRST open (opens 0→1) and posts to the org's existing digest chat webhook via `_sap_post_chat` ("👁 Shared governance digest opened …token"). Backend-only; best-effort (no-op if no webhook).
- **Share Briefing** — `POST /api/sap/digest/share-briefing {recipients[]}` creates a share, generates the voice mp3 (cfg voice/speed), and emails ONE message = `_governance_digest_html(data, share_url)` body + `.mp3` attachment. UI: `digest-share-briefing` button (recipients prompt prefilled from digest recipients); refreshes the shares list.
- **Weekly Recap** — opt-in `recap_enabled` + `recap_day` in the digest config (validated against `_WEEKDAYS`). `run_sap_weekly_recap()` (registered in `scheduled.py` daily cron, gated by `weekday==recap_day`) aggregates the last 7 days of `sap_digest_chat` user questions (`_weekly_recap_data`), emails the top-6 most-asked to digest recipients (`_weekly_recap_html`). UI: `recap-config` block (`recap-toggle` + `recap-day`).
- iter66 fix: the insert_text end-of-file append landed mid-`digest_voice` (earlier same-batch edits had shifted the line count); reconnected the function head to its body and removed the orphaned tail. Lesson: after same-batch edits shift line numbers, append to the true EOF (re-grep) rather than a stale line number.

## SAP UAC — Slack Ask (inbound slash-command) — DONE (Jun 2026)
Leadership can ask governance-digest questions directly from Slack via a slash-command (e.g. `/askdigest what are the top risks right now?`); the AI answers in-channel, grounded in the live SAP access snapshot (No-Mock, real HMAC verification).
- **Backend** (`sap_digest.py`): refactored the in-app ask logic into reusable `_run_digest_ask(org_id, question, session_id, actor, channel)`; `POST /api/sap/digest/ask` is now a thin wrapper. NEW public `POST /api/sap/slack/ask` (no auth dependency): verifies Slack request authenticity with real HMAC-SHA256 over `v0:{ts}:{raw_body}` using the org's configured signing secret (`_verify_slack_sig`), rejects stale (>5 min) requests (401). The signing secret doubles as workspace identity — `_resolve_slack_org` finds the org whose secret validates the signature (multi-tenant safe). Immediately acks with an ephemeral "Analyzing…" then answers via a `BackgroundTasks` POST to Slack's `response_url` (in_channel blocks); empty text → usage help with suggestions; missing response_url → inline synchronous answer. Captures `team_id` on first call.
- **Config**: `_DIGEST_DEFAULT`/`DigestConfigBody` gained `slack_ask`, `slack_signing_secret`, `slack_team_id`. GET `/digest/config` NEVER returns the raw secret (masked to "") and adds `slack_signing_secret_set: bool`. PUT preserves the existing secret when the incoming value is blank (so saving other settings never wipes it).
- **Frontend** (`SodCommandCenter.jsx` digest card, `slack-ask-config`): toggle (`slack-ask-toggle`) reveals the copy-able slash-command Request URL (`slack-ask-url` = `${REACT_APP_BACKEND_URL}/api/sap/slack/ask`, `slack-ask-copy`), a password Signing Secret field (`slack-ask-secret`, shows "· configured" + "leave blank to keep current" when already set), and setup guidance.
- VERIFIED: `tests/test_slack_ask.py` (real HMAC) — valid signed → ephemeral ack; empty text → suggestions; WRONG secret → "not linked" (org unresolved, proves HMAC); stale ts → 401; no response_url → inline grounded answer ("28 open Critical…"). Frontend screenshot confirms the card renders + URL populated. Demo org left with slack_ask OFF / secret cleared.

## SAP UAC — Ask-channel polish + Teams Ask + perf (Jun 2026, iteration_68 — backend 15/15, frontend 100%)
Follow-up batch after Slack Ask. All No-Mock (real HMAC, real LLM grounded answers).
- **Phase 2 was already done**: every SAP UAC page (all 15) already renders the Obserra-standard auto AI Analyst card via `components/SapInsight.jsx` → `GET /api/sap/insight` (grounded in the live access model). No build needed — only perf tuning (below).
- **Send a test question** (`POST /api/sap/slack/test`, admin): runs the grounded ask, posts to the org's Slack webhook if configured; returns `{answer, model, signing_secret_set, webhook_configured, webhook_posted}`. UI button `slack-ask-test` + `slack-ask-test-result` panel on the Slack Ask card.
- **Command shortcuts** (`_ASK_SHORTCUTS` + `_expand_shortcut`): a Slack/Teams user can type just a keyword (`top risks`, `score`/`score trend`, `critical`, `residual`/`residual access`, `priorities`, `auto`) which expands to a full grounded question. Chips shown on the card (`slack-ask-shortcuts`); also listed in the empty-text Slack help.
- **Answer Log** (`db.sap_ask_log`, `GET /api/sap/ask-log?source=&limit=`): every Slack/Teams/test Q&A (who asked + answer + model + time) logged in `_log_ask`; surfaced on Workflow Activity (`ask-log-card`/`ask-log-list`) with a `by_source` (Mongo aggregation) count line.
- **Teams Ask** (`POST /api/sap/teams/ask`, PUBLIC): Microsoft Teams Outgoing Webhook. Real HMAC — `base64(hmac_sha256(base64decode(teams_ask_secret), raw_body))` compared to `Authorization: HMAC <b64>`; org resolved by whichever configured secret validates. Answers synchronously (`{"type":"message","text":…}`, 8s LLM budget via `_run_digest_ask(timeout=…)`); strips `<at>…</at>` mentions; empty → suggestions. Config: `teams_ask`, `teams_ask_secret` (write-only, masked, `teams_ask_secret_set`), `teams_ask_id`. UI: `teams-ask-config` card on the SoD Command Center (mirrors Slack — callback URL `.../api/sap/teams/ask`, copy, HMAC secret, guidance).
- **Performance (Obserra parity)**: `GET /api/sap/insight` now cached 180s per (org, focus) in `_SAP_INSIGHT_CACHE` — cold ~5s LLM → cached ~0.13s (40×). The AI card streams async and never blocks page render; `_correlate`-backed endpoints already ~0.11s. Mobile (390px) verified: shortcuts stack, cards fit, inputs truncate, no horizontal overflow.
- VERIFIED iteration_68: backend 15/15 pytest (`tests/test_ask_channels.py` + tester's `test_iter68_ask_channels.py`), frontend e2e (Slack+Teams setup + test-result + Workflow ask-log + persistence), 14-route regression 0 pageerrors, insight cache confirmed. Demo org reset: slack_ask & teams_ask OFF, both secrets cleared.

## SAP UAC — Multi-turn ask, rate-limit, Teams test, Ask Analytics + Obserra-parity AI Fix (Jun 2026)
User directives: "make everything like Obserra — cards open with action buttons + AI recommends how to fix", "brand must be consistent", "save this format for the next app" (see `/app/memory/OBSERRA_PATTERN.md`).
- **Multi-turn conversation (no new credentials)**: Slack keys the session by `team+channel+user`, Teams by `conversation.id` — consecutive `/askdigest` (or @mention) questions continue the thread via `_run_digest_ask` history. `reset`/`new`/`clear` starts a fresh thread. (Full threaded bot replies would need Slack Events API + bot token / Teams Bot Framework — deferred.)
- **Abuse guard**: `_rate_limited` in-memory sliding window (20 req / 60s per org) on the public `/slack/ask` + `/teams/ask` AFTER HMAC verification; returns a friendly throttle message. Verified 9/26 throttled.
- **Teams test button**: `POST /api/sap/teams/test` (admin) mirrors slack/test — grounded answer + posts to `teams_url` (MessageCard) if set; UI `teams-ask-test` + `teams-ask-test-result` on the Teams Ask card.
- **Ask Analytics**: `GET /api/sap/ask-analytics` → `{total, by_source, top_questions[], top_askers[]}` (Mongo aggregation); shown on Workflow Activity (`ask-analytics`, `ask-top-q-*`, `ask-top-user-*`). Also fixed a dropped `_log_ask` in the Slack background responder so real (response_url) Slack answers are logged.
- **Obserra-parity AI Fix (the big one)**: NEW `POST /api/sap/fix {entity, ref}` (entity ∈ identity|conflict|role|account) → grounded `{rating, score, rationale[], recommendation, steps[], model}`, cached 180s per entity, deterministic fallback. Reusable `components/SapAIFix.jsx` (mirrors `AIFix.jsx`). Wired into the bespoke detail modals that lacked it: **Identities** (`entity=identity`) and **RoleIntelligence** (`entity=role`). Pages using the shared app-wide `RiskDetailModal`/DeepDive (SodCommandCenter, PrivilegedAccess, AccessMonitoring, Lifecycle, SapOverview) already show AI brief + grounded recommendations + action hub — confirmed at parity. Every SAP page already has the top `SapInsight` AI card.
- Test harness: `tests/test_ask_multiturn.py` (multi-turn, reset, rate-limit, teams-test, analytics). `/api/sap/fix` verified via curl (identity P-0008 Critical 87 grounded; conflict Critical 92; cache 0.09s; bad ref 404).





## SAP UAC — SapAnalytics drilldown crash fix (Jun 2026, iteration_70)
- **P0 fix**: `SapAnalytics.jsx` called `useDeepDive()` but was missing `import { useDeepDive } from "@/context/DeepDiveContext";` — the page crashed on render (ReferenceError), which is why the earlier screenshot test timed out waiting for `an-sod-area-item-0`. Added the import (matching every other page). This completes **Phase B** (analytics chart drilldowns → universal deep-dive with grounded AI recommendations + Apply/Add-to-plan).
- **VERIFIED**: login 200, `GET /api/sap/analytics` 200 (sod_by_area=6, top_roles=10); screenshot flow login → /app/analytics → click `an-sod-area-item-0` opens the deep-dive modal ("Basis / Security — 22 open SoD conflict(s)", Critical 99/100, compliance chips + recommended actions + Add to remediation plan). Frontend compiles clean; all services RUNNING.
- The full "Obserra-parity / Apply-this-fix" batch (Phase A inline actions, Phase B analytics drilldowns, Phase C weekly Ask digest) is now COMPLETE and previously passed testing at iterations 68–69.
- **P2 Component Refactoring** (split `SodCommandCenter.jsx` ~1,191 lines + `sap_digest.py` ~2,129 lines) — DEFERRED at user request (user chose to keep the app as-is and confirm green rather than refactor now). Still recommended for long-term maintainability.

## SAP UAC — Analytics Explorer + Risk Watchlist + Safe Refactor (Jun 2026, iteration_70)
User requested three items; all delivered and verified (testing_agent iteration_70: backend 20/20, frontend 100%, no issues).
- **Analytics Explorer**: `GET /api/sap/analytics` now accepts optional `region`/`department` query params that scope EVERY metric to that slice, and returns a `filters:{regions,departments,region,department}` object. Frontend `SapAnalytics.jsx` gained an "Explore" filter bar (region + department dropdowns, active-slice summary, Clear). (Fixed a param/local-var shadowing bug where the aggregation dict `region` clobbered the `region` filter param → renamed local to `region_agg`.)
- **Risk Watchlist**: per-auditor pinned SoD business areas. New endpoints `GET/POST/DELETE /api/sap/watchlist` (collection `db.sap_watchlist`, keyed by org_id+user email+area; POST validates against known SoD areas → 404 otherwise). New self-contained `components/SodWatchlist.jsx` (uses `useDeepDive`, syncs via a `sap-watchlist-changed` window event) mounted at the top of BOTH `/app/analytics` and `/app/sod`. Pin stars added to the analytics "by-area" bars; watchlist cards show open-conflict counts + severity chips, hottest-first, clickable into the deep-dive.
- **Safe Refactor (backend)**: `sap_digest.py` 2,129→1,207 lines. Extracted `sap_ask.py` (631 — Ask/Slack/Teams/analytics/export/history/rename) and `sap_evidence.py` (333 — SoD Evidence Pack). Both attach to the shared `sap_router`; `sap_digest.py` imports them at the bottom and re-exports `_digest_ai_context` (used by `_build_digest_snapshot`) and `run_sap_sod_evidence_export` (used by the scheduler) — the re-export chain to `sap_uac`/`scheduled.py` is preserved. Byte-accurate anchor-based extraction (no dropped lines).
- **Safe Refactor (frontend)**: `SodCommandCenter.jsx` 1,191→836 lines. Extracted `components/sod/GovernanceDigestCard.jsx` (354 — digest schedule, score-drop alerts, evidence pack, Slack/Teams Ask, voice, share) and `components/sod/sodPrimitives.jsx` (SEV, Chip, ScoreTile, ACTION_LABEL, TrendTip). GovernanceDigestCard receives its 53 props (auto-computed) from the parent, which retains all state.
- **Advisory backlog (from testing_agent, not bugs)**: `sap_uac.py` (~2,138 lines) is now the largest backend file — consider peeling analytics/watchlist into `sap_analytics.py`. `SodCommandCenter.jsx` still >700 lines — Scorecard/Auto-Rem/Simulator/Rule-Library/Conflicts-table are next extraction candidates. GovernanceDigestCard's 53 props are brittle — consider consolidating into one `digest` state object. SodWatchlist's window-event sync could move to a shared store/context.

## SAP UAC — Analytics Export + Watchlist Alerts/Remediation + more splits (Jun 2026, iteration_71)
All four backlog picks + the enhancement delivered and verified (testing_agent iteration_71: backend 18/18, frontend 100%, zero issues, no missing-prop errors).
- **Analytics Export**: `GET /api/sap/analytics/export?region=&department=&format=csv|pdf` — branded CSV (KPIs + breakdown sections) and a branded reportlab PDF (Obserra navy header, KPI grid, area/dept/region/roles/top-risk tables), both honoring the active slice. Frontend `SapAnalytics.jsx` got `CSV`/`PDF` buttons (`an-export-csv`/`an-export-pdf`) that download the current slice.
- **Watchlist Alerts**: per-pin `alert` + `threshold`; `POST /api/sap/watchlist/alert` enables/disables (enabling also pins). New daily cron `run_sap_watchlist_alerts` (piggybacked on the existing daily sweep in `scheduled.py`, no new cron entry) emails the pin owner + files an in-app notification when open Critical conflicts ≥ threshold, one nudge per ISO week (auto-resets when it clears). Frontend: bell toggle (`watchlist-alert-N`) + threshold input (`watchlist-threshold-N`) on each card.
- **Enhancement — one-tap remediation**: `POST /api/sap/watchlist/remediate {area, owner}` assigns an owner and opens a ServiceNow-orchestrated change (via `_snow_generic`) for the area's conflicts; owner + ticket persist on the pin. Frontend: `watchlist-assign-N` → owner input → `watchlist-remediate-N` → ticket badge (`watchlist-ticket-N`) + Reassign. (Tickets auto-close end-to-end per the app's convention.)
- **Analytics Split**: moved analytics + watchlist backend code out of `sap_uac.py` (2,139→2,000 lines) into new `sap_analytics.py` (~402 lines). `sap_uac.py` imports it at the bottom and re-exports `run_sap_watchlist_alerts`; the re-export chain to `scheduled.py` is preserved. Fixed a lint-caught `_WEEKDAYS` import in `sap_evidence.py`.
- **Card Slimming**: `SodCommandCenter.jsx` 836→640 lines — extracted `components/sod/SodScorecardCard.jsx`, `SodAutoRemCard.jsx`, `SodToolsRow.jsx` (Simulator + Rule Library), `SodConflictsTable.jsx` (each receives auto-computed props; byte-accurate, no behavior change).
- **Advisory (from testing, not bugs)**: consider consolidating each extracted card's pass-through props into a single state object + setter, and moving SodWatchlist's `sap-watchlist-changed` window-event sync to a shared context/store.


## SAP UAC — Owner/Board crons verified + Ticket Deep-Link + "Assigned to me" lens + SoD state consolidation (Jun 2026, iteration_72 — frontend 100%, backend curl/script-verified)
Final batch of the user's 4-item backlog delivered (user chose "all three now"; lens matches on logged-in email):
- **Owner Digest + Monthly Board Pack crons (backend, verified)**: `sap_analytics.py` `run_sap_owner_digest()` (weekly — each watchlist owner is emailed only the SoD areas assigned to them, with live Critical/open counts + linked ticket numbers) and `run_sap_board_pack()` (monthly, once/calendar-month per `sap_board_pack_log`, opt-in via digest `board_pack` flag — branded exec HTML summary + the SAP analytics PDF attached to `recipients`). Both fold into the existing daily cron. Verified via `/app/backend/_t_crons.py` (board-pack log created, owner digest ran clean, no Resend/PDF errors). Test script's standalone circular-import fixed by importing `sap_uac` first.
- **Ticket Deep-Link timeline modal (frontend, NEW)**: the ServiceNow ticket badge on each SoD Risk Watchlist card (`watchlist-ticket-<idx>`) is now a clickable button → opens `watchlist-ticket-dialog`, fetching `GET /api/sap/ticket/{number}` (`sap_analytics.py:405`, returns the `sap_snow_tickets` doc incl. `stages[]`). Renders the full color-coded stage timeline (`watchlist-ticket-stage-N`: NEW→WORK NOTE→IN PROGRESS→system fan-out→RESOLVED→CLOSED), meta chips (state/type/person/systems_touched), reason, and a duration footer ("closed end-to-end in Ns").
- **"Assigned to me" watchlist lens (frontend, NEW)**: header toggle `watchlist-mine-toggle` ("Assigned to me (N)") filters the pinned grid to only areas whose `owner` equals the logged-in user's email (via `useAuth()`); `watchlist-mine-empty` state when none match. Count badge shows how many pinned areas the owner is accountable for.
- **SoD State Consolidation (frontend refactor, P1)**: replaced the ~50+ prop pass-throughs from `SodCommandCenter.jsx` to its 5 extracted cards with a shared React context — NEW `src/context/SodContext.jsx` (`SodProvider` + `useSod`). Parent builds one `sodCtx` object and wraps the tree; `SodScorecardCard`, `SodAutoRemCard`, `SodToolsRow`, `SodConflictsTable`, `GovernanceDigestCard` now read `useSod()` instead of destructuring props (JSX bodies untouched → zero-risk). Full `testing_agent` regression confirmed NOTHING dropped.
- VERIFIED iteration_72: frontend 100% (all cards render live: 28/40/3/72 KPIs, scorecard, auto-rem, digest schedule, simulator, rule library, conflicts table + filters/mitigate/simulate/rule dialogs), both new watchlist features end-to-end (CHG100577 6-stage timeline; lens keeps Finance visible for admin owner). Backend skipped (already verified). Only open note: pre-existing cosmetic `<span>`-in-`<option>` dev-instrumentation console warning on the native watchlist-add select (not introduced here).


## SAP UAC — Owner Leaderboard + Board-Pack on-demand + Ticket live-refresh + Digest deep-links (Jun 2026, iteration_73 — frontend 100%, backend curl-verified)
Follow-up 4-feature batch on the SoD Command Center (user: "yes, build all four"):
- **Owner Accountability Leaderboard** (`components/sod/SodOwnerLeaderboard.jsx`, `GET /api/sap/watchlist/leaderboard`): ranked "who owns the most open Critical SoD" across all assigned watchlist owners in the org. Aggregates `sap_watchlist` docs by `owner` (deduped per area), each with Critical/open/area-count + a red workload bar; expandable per-area breakdown with ticket numbers. Summary tiles: owners accountable, areas-with-owner / total, and **unowned open Critical** (red when >0). Verified: admin owns Finance (12 Critical / 18 open), 16 unowned Critical.
- **Board Pack on-demand** (`components/sod/SodBoardPackCard.jsx`, `GET /api/sap/board-pack/preview` + admin `POST /api/sap/board-pack/send`): preview THIS month's branded exec board pack (posture + hottest areas + risk movers + 30-day wins) with the analytics PDF; admins can send immediately (records the monthly `sap_board_pack_log` so the 1st-of-month cron won't double-send). `_board_pack_build`/`_board_pack_recipients` helpers refactored out of the cron. Send curl-verified (sent:1 → already_sent flips True).
- **Ticket timeline Live Refresh** (`SodWatchlist.jsx`): while the ServiceNow ticket modal is open it re-fetches `GET /api/sap/ticket/{number}` every 4s so an in-flight change advances without reopening; header shows an "auto-refreshing" pulse chip (`watchlist-ticket-live`).
- **Owner Digest Deep-Links** (`_owner_digest_html` in `sap_analytics.py` + `SodWatchlist.jsx`): each area row in the weekly Owner Digest email now links to `{FRONTEND_URL}/app/sod?wl=<area>`; the watchlist reads `?wl=` and scrolls to + highlights (primary ring) that pinned card.
- Bugs caught & fixed this session (known silent-partial-edit recurrence): one child prop-spread cleanup and the `WatchlistCard` `cardRef` declaration each reverted mid-batch and were re-applied as isolated edits (root cause of a transient `cardRef is not defined` runtime error, resolved).
- VERIFIED iteration_73: frontend 100% (all 4 features end-to-end + full SoD regression green). Only open note: the pre-existing cosmetic `<span>`-in-`<option>` dev-instrumentation console warning on the native pin-area select (dev-only; not emitted in production build).


## SAP UAC — Leaderboard Nudge + Assign-from-Leaderboard + Board-Pack Scheduling + Timeline Toast (Jun 2026, iteration_74 — frontend 100%, backend curl-verified)
Second follow-up 4-feature batch on the SoD Command Center (user: "yes, all four"):
- **Leaderboard Nudge** (`POST /api/sap/watchlist/leaderboard/nudge`, admin): one-tap "Nudge all owners" emails every SoD-area owner their assigned hot spots now (same content as the weekly Owner Digest, on demand). Frontend button has a `confirm()` gate to prevent accidental mass emails.
- **Assign From Leaderboard** (`SodOwnerLeaderboard.jsx` + leaderboard `unassigned` list): the leaderboard now returns unowned areas (open>0, no owner); each renders with an owner-email input + Assign button → `POST /api/sap/watchlist/remediate` (assigns owner + opens a ServiceNow ticket), then live-refreshes the board via the `sap-watchlist-changed` event (owner count ↑, area leaves the unowned list).
- **Board Pack Scheduling** (`sap_board_pack_config` collection + `GET/PUT /api/sap/board-pack/config`, admin PUT): admins set monthly auto-send on/off, day-of-month (1–28, clamped), and a recipient override (blank = digest list) inline on the Board Pack card; a header chip shows "Auto-send · day N". Isolated from the digest config so neither writer clobbers the other. `run_sap_board_pack` now gates on `settings.enabled AND today == settings.day` (still month-deduped).
- **Timeline Toast** (`SodWatchlist.jsx`): while the ticket modal live-refreshes (4s), if a new stage appears it toasts "CHG… advanced → <state>". (Demo tickets auto-close, so it stays silent+stable for them — verified no flicker/crash.)
- VERIFIED iteration_74: frontend 100% across all 7 scenarios + full SoD regression. Baseline reseeded afterward (test-created ops-owner@acme.com assignment + CHG100578 removed, board-pack config reset; Finance pin owner=admin/CHG100577 kept).


## SAP UAC — Walkthroughs & Setup Guides rebuilt with screenshots (Jun 2026)
Re-pointed the existing "Obserra standard" documentation pipeline from the old EIOS product to SAP UAC:
- **Screenshot capture** (`scripts/capture_shots.py`): rewritten to log in (auth form on `/`) and capture 14 SAP UAC screens at 1440×900 — sign-in, Executive Overview, SAP Analytics, SoD Command Center (+ a scrolled shot of the Risk Watchlist / Owner Leaderboard / Board Pack), Privileged Access, Access Monitoring, Identities, Joiner/Mover/Leaver, HR Reconciliation, Role Intelligence, Access Requests, Certifications, Settings. Playwright Chromium (build 1234) was reinstalled to `/pw-browsers`.
- **Guide content** (`scripts/gen_docs.py`): SECTIONS fully rewritten for SAP UAC → title "Obserra SAP UAC — Install & User Guide", About, PWA install, on-prem Docker, sign-in, and a screenshot-backed walkthrough of every dashboard incl. the new SoD Watchlist/Owner-Leaderboard/Board-Pack features. Outputs PDF (16 pages) + DOCX (14 embedded images) to `backend/assets/docs/`. Verified by rendering the PDF pages.
- **Download/email copy** (`deploy.py`, `Settings.jsx`): user-facing filenames → `Obserra-SAP-UAC-Install-and-User-Guide.pdf/.docx`; email subjects/HTML and Settings "Deployment & Documentation" copy updated to SAP UAC. On-disk paths + `regenerate_guides()` (server startup + weekly `scheduled.py` cron) unchanged, so guides auto-refresh.
- **In-app guided tour** (`OnboardingTour.jsx`): EXEC/OPS steps rewritten for SAP UAC (SoD Command Center focus, targets `nav-sod-command-center`). Mechanism unchanged (`if (!open) return null`); auto-shows on first login, replayable from Settings.
- Verified: `GET /api/deploy/guide.pdf` (1.47 MB) and `/guide.docx` (0.97 MB) return 200 with correct content-types; PDF pages render with SAP UAC screenshots.


## SAP UAC — On-prem one-click installer + hardening (Jun 2026, this fork)
Continued from the docs work; all curl + screenshot verified on the live app.
- **In-app tour fix**: `<OnboardingTour />` was never mounted anywhere → mounted in `AppShell.jsx`; auto-show, Settings "Replay tour", and step preview images now work.
- **Refresh tour images / Refresh all visuals**: `SHOT_TOUR_ONLY` mode + `POST /api/deploy/regenerate-tour`; "Refresh all visuals" now runs in the background (`POST /api/deploy/refresh-visuals`) and posts a "Visuals updated" in-app notification. Hardened `capture_shots.py` with `_settle()` (networkidle + Suspense spinner clear) to stop blank captures.
- **Self-contained one-click installer**: `_build_onprem_zip()` bundles full `backend/`+`frontend/` source + deploy files + `.dockerignore` + executable `install.sh` (shared `scripts/onprem_pack.py`); rebranded to "Obserra SAP UAC"; fixed broken Docker build contexts across plain/Caddy/Traefik compose; `install.sh` auto-generates JWT_SECRET, launches, health-checks, and creates the first admin.
- **Version stamp**: `VERSION` (1.0.0) + `BUILD_INFO`; version-stamped download filename; `OBSERRA_VERSION` env + `scripts/stamp_version.py` (git-tag bump).
- **Offline mode**: bundled `emergentintegrations` wheel + `build-wheelhouse.sh` + `OFFLINE` marker for air-gapped builds.
- **Seed admin / seed sample data**: `bootstrap-status` + `bootstrap-admin` (first-run only, 409 in hosted) with a `seed_demo` flag; `POST /api/deploy/seed-demo` loads the realistic SAP dataset (46 persons / 54 accounts).
- **Health endpoint**: `GET /api/health` (`{status,service,version,db}`), used by `install.sh`.
- **Update notifier**: `GET /api/deploy/version` (+ `UPDATE_MANIFEST_URL`, UA header to avoid 403) drives an admin-only dismissible `UpdateBanner.jsx`.
- **CI/release**: `scripts/assemble_onprem.py` + `Makefile` + `.github/workflows/onprem-verify.yml` (docker build on push) and `release.yml` (tag → stamp, GHCR image push, versioned `.zip` release asset); `docker-compose.ghcr.yml` runs prebuilt images. Test creds unchanged (`jblan2026@gmail.com`); see `test_credentials.md`.
- **Ops follow-ups**: release-notes modal in `UpdateBanner`; one-click `POST /api/deploy/upgrade` (opt-in `ONPREM_UPGRADE=1`, else 400); deep `GET /api/health` (db latency, orgs, connectors, scheduler); `POST /api/deploy/reset-demo` + Settings "Reset to demo dataset"; `release.yml` also publishes a GitHub Pages `manifest.json` and pre-fills `UPDATE_MANIFEST_URL` so update banners work out-of-the-box. All curl + screenshot verified.


---
## Status update — Jun 2026 (System Health suite complete)
System Health admin page (`/app/system-health`) now ships: deep-health tiles + amber/green header pill, one-click on-prem upgrade w/ streaming log, nightly backups (schedule: enabled/daily-weekly/keep) with **AES-at-rest encryption** (org passphrase, JWT_SECRET-wrapped key; passphrase required to restore), pre-restore auto-backup + typed-RESTORE confirm, backup download guardrail (org/contents/tag/lock), degraded-connector drill-down w/ one-click re-probe, 24h/7d/30d uptime strip, and health-alert routing matrix (DB/Connector/Scheduler × Slack/Teams/Email) with a Send-test-alert button. Backend in `deploy.py`; alerts fold into the daily cron. See CHANGELOG.md for details.

## SAP UAC — SLA targets + audit-request analytics/bulk + perf pass (Jun 2026, iteration_85 — backend 13/13, frontend 100%)
Final audit-workflow batch + optimization. All P0/backlog for the auditor experience is now DONE.
- Configurable **Response SLA** (org default `system_health.sla_hours` + per-room override) drives the overdue-request digest (was hardcoded 72h) and a new `sh-sla-panel` UI. Endpoints: `GET/PUT /api/deploy/sla-config`, `PUT /api/deploy/audit-room/{token}/sla`.
- **Action-link expiry**: digest-minted Slack/Teams resolve/reply tokens expire after 7 days (`action_expires_at` + `_action_expired`).
- **Weekly-median response-time sparkline** in the Audit Requests analytics strip (`sh-request-trend`, from `/audit-request-analytics` `trend[]`).
- **Select-across-filters** bulk action (`sh-select-across` → `POST /api/deploy/audit-room-comments/bulk-status-filter`, `update_many` across the whole matching set).
- **Performance**: route-chunk prefetch on nav hover (`src/lib/routePrefetch.js`); client-side logo downscale (≤512px) before upload; on-prem zip build cached by source mtime; guide downloads carry cache headers (ingress-overridden in hosted). Backend reads already ~90–130ms + gzip.
- Backlog after this: none outstanding for the auditor/System-Health workstream. Optional tech-debt: split `deploy.py` (~2790 lines) and `SystemHealth.jsx` (~1430 lines) into modules; clean the pre-existing span-in-option hydration warning in SodWatchlist.


## SAP UAC — Escalation + Heatmap + Digest Schedule + code split (Jun 2026, iteration_86 — backend 22/22, frontend 100%)
Follow-up batch complete. Auditor/System-Health workstream is fully delivered.
- **SLA Escalation** (`escalation-config`): overdue requests past 1.5× SLA escalate to org contacts (email + Slack/Teams), deduped.
- **SLA Heatmap** (`audit-request-analytics` + `SlaHeatmap`): per-room on-time/breached/pending with on-time %.
- **Digest Scheduling** (`digest-schedule`): per-org enable + days-of-week (Mon–Sun). Runs on the daily 08:00 UTC platform cron — per-hour not possible (platform 5-cron cap already reached).
- **Code split**: `deploy.py` → `deploy.py` (core) + `deploy_audit.py` (auditor governance); `SystemHealth.jsx` presentational pieces + `SlaHeatmap` → `src/components/systemhealth/panels.jsx`.
- Backlog / P2 (optional): deeper `SystemHealth.jsx` state-level split (deferred — audit & evidence share panels/modals); clean pre-existing span-in-option hydration warning in SodWatchlist; add per-hour digest scheduling if a cron slot is freed.


## SAP UAC — Heatmap drilldown + escalation rollup + per-hour digest + Exec banner (Jun 2026, iteration_87 — backend 16/16, frontend 100%)
Final follow-up batch complete; user asked to package + finish (no more features).
- **Heatmap Drilldown**: clickable per-room heatmap rows jump to that room's overdue requests (analytics now returns room `token`).
- **Escalation Weekly Rollup** (`weekly-drift-digest` cron): owners get a 7-day summary of escalations + median escalation→resolution time; `resolved_at` now stamped on all resolve paths.
- **Per-Hour Digest**: `digest_schedule.hour` (0-23 UTC) via new `hourly-overdue-digest` cron; freed a slot by folding monthly Studio report into `monthly-board-report` (still 5 crons max).
- **Exec Overview SLA banner**: `audit-sla-banner` endpoint + `sap-sla-banner` on /app (shown when open-overdue/breached-7d/escalated-7d > 0).
- Backlog / P2 (optional, none requested): split `deploy_audit.py` (~1300 lines) into submodules; clear the pre-existing dev-overlay span-in-option console warning.

